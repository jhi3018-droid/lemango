# 르망고 상품 관리 시스템 — CLAUDE.md

> 순수 HTML + CSS + JavaScript (프레임워크 없음). 로컬 서버: `python -m http.server 8765`. Firebase Hosting(`lemango-office`) + Auth + Firestore + Storage.
> 이 파일은 **살아있는 규칙(1~4장)을 최상단**에 두고, 완료 이력은 5장 압축 changelog로 유지한다. 상세 play-by-play는 git 이력 참조.

---

# 1. 살아있는 규칙 & 정책 (Living Rules & Policies)

## 1.1 📋 보고서 형식 규칙 (MANDATORY)
**모든 보고서·작업 결과·설계 문서는 반드시 아래 시작 마커로 시작한다.** 소유주가 영문을 못 읽어 CC 결과물을 Claude.ai에 붙여 한글 요약함 → 작업 로그와 실제 결과물이 섞이므로 시작 지점 표시 필수.

시작 마커(필수):
```
========================================
📋 작업 결과 / REPORT START
========================================
```
종료 마커(권장):
```
========================================
✅ 작업 결과 끝 / REPORT END
========================================
```
- 사용: 모든 작업완료 보고서 · 조사/설계 문서 · Claude.ai에 붙여넣을 결과물. 한 줄 확인 응답엔 불필요(있어도 무해).
- 한글 문구(`작업 결과`)는 반드시 포함 — 소유주가 시작 지점을 즉시 알아보기 위함.

## 1.2 🔐 POS 권한 방침 (POS 전 단계 필수 준수 — 확정/불변)
원칙: **조회(보기)=전 직원 개방 · 작업(등록/취소/차감)=본인 매장 직원만 · 관리자(grade≥3)=전 매장 전권.**

| 기능 | 권한 |
|------|------|
| 매출 조회 | **전 직원** (회사 매출 전원 공유) |
| 매장별 재고 조회 | **전 직원** (회사 공용 데이터) |
| POS 판매 등록 | **본인 매장 직원만** (사무직/미배정 불가) |
| 판매 취소/환불 | **본인 매장 직원만** + 🔴 **로그 + 사유 필수** |
| 강제 재고 차감 (불량/오류) | **본인 매장 직원만** + 🔴 **로그 + 사유 필수** |
| 관리자 (grade≥3) | **위 전부 + 모든 매장** |

### 🔴 감사 로그 (민감 작업 필수 — 가장 중요)
**판매 취소(void) · 환불(refund) · 강제 재고 차감**은 반드시 기록:
- **누가(WHO) · 언제(WHEN) · 무엇을(WHAT) · 사유(REASON)** 4가지 모두 기록.
- **사유는 필수 입력** — 빈 사유 → 작업 차단.
- 방식: `logActivity(action, target, detail)` (activity-log.js) 감사 추적 + 해당 작업을 **자체 레코드로도 저장**(사유 필드 포함).
- 목적: 사후 정산·추적("이 재고가 왜 사라졌는가?"). 없으면 재고 불일치 추적 불가.

> 조회 화면(매출·재고)은 전원 접근, **작업 화면(판매·취소·차감)만 본인 매장 직원+관리자로 게이트**. 취소/환불/차감 세부 UI/흐름은 추가 지시 예정 — 단 위 권한·로그·사유 방침은 확정(불변).

## 1.3 Working Rules (확립된 작업 규칙)
- **배포는 소유주 수동이 기본** — 단, 소유주가 명시 요청 시 배포·푸시 모두 수행 OK.
- **묻지 말고 결정·보고** — 최선 판단으로 반영 후 변경 요약. 애매하면 보수적으로 판단.
- **레거시 데이터 인지** — localStorage/Firestore 스키마 변경 시 변경 전 레코드가 새 코드와 다르게 동작할 수 있음을 반드시 명시.
- **대시보드 모달 재사용** — 신규 대시보드 프리뷰는 `dashInfoModal` 프레임 재사용(새 dialog 세우지 말 것).
- **헤더 span 너비 ≡ CSS 컬럼 너비** — 모달 내 표(`.ps-phase-*` 등) 손댈 때 `<span>` inline 너비와 CSS 클래스를 함께 수정(안 하면 헤더-행 desync).
- **`makeDraggableResizable()`는 `.srm-header`/`.srm-modal-header`/`.rmodal-header`/`.dmodal-header` 필요** — 신규 draggable dialog는 타이틀바에 이 클래스 중 하나 포함.
- **알림 링크는 refId 필수** — `#tab:id` (또는 `#tab:personal:id`); 절대 `#tab`만 저장 금지.
- **선택적 git 스테이징** — `git add js/ index.html style.css firebase.json CLAUDE.md` (harness 파일 제외). `.claude/settings.local.json`·`.firebase/` 등 스테이징 금지.
- **신규 탭 추가 시 `TAB_PERMISSIONS`에 반드시 등록** — 미등록 탭은 `canAccessTab`이 true(전체개방) 반환.
- **CSS `<dialog>` 함정**: `.srm-modal{display:none}` / `[open]{display:flex}`가 열림 제어 → 추가 클래스에 `display` 주지 말 것(닫힌 dialog가 보이는 버그). `inb-hidden`류 숨김 클래스와 `display:flex` 클래스가 equal-specificity로 충돌하면 전용 `.x.hidden{display:none}`(특이도 up) 규칙 추가.
- **숨김 탭 재렌더 금지** — `display:none` 테이블 렌더 시 sticky 헤더/컬럼 너비 측정(getBoundingClientRect=0) 깨짐. dirty-flag 후 전환 시 렌더.

## 1.4 🔴 매출 공식 (절대 변경 금지)
- **Cafe24: 매출 = P(전체합산) + Q(전체합산) − U(주문번호당 MAX 1회) − Y(전체합산)**
  - P=상품구매금액(per-item SUM), Q=총배송비(SUM), U=실제환불금액(주문번호당 MAX 후 SUM — FIRST는 부분환불 누락), Y=상품별추가할인(SUM). W=적립금(주문당 FIRST, 순실결제=매출−W).
  - 검증값(2026-01): 300,281,100 + 3,399,000 − 8,329,593 − 87,712,735 = **207,637,772**.
- **사방넷: 매출 = H(결제금액) + I(배송비)** — **사은품 행 제외** 후 전행 합산.
  - 사은품 판별: C열(주문번호)에 `_사은품` 포함 OR D열(자체코드)에 `사은품` 포함. 사은품 H=0, I는 본품과 중복기재 → 포함 시 과대계상.
  - 검증값(2026-01): 26,607,452 + 1,035,000 = **27,642,452**.

## 1.5 🏆 품질 기준 (QUALITY BAR — 전 작업 예외 없음)
1. **고치기 전에 진단.** 버그: 실제 데이터/로그/코드를 **먼저** 읽고, 근거(file:line, 실제 doc 값)와 함께 root cause를 명시한 **후** 수정. 추측-패치 금지.
2. **STOP-and-report 트리거**(작업 중단 → Claude.ai 보고, 절대 임의 진행 금지):
   - 승인된 설계 문서의 스키마/메커니즘에서 벗어나는 모든 이탈
   - 작업 중 예상치 못한 firestore.rules / index / 데이터모델 변경 필요
   - 아키텍처 충돌 발견(예: split-line vs void-keying) — **코드 쓰기 전** 중단
   - 작업지시의 보장(atomicity, exactly-once, byte-unchanged)을 충족 못할 때
3. **code-reviewer 게이트 필수**(🟢/🟡/🔴). 리뷰어는 해당 작업의 "critical properties"(작업지시가 명명)를 **명시적으로 검증**해야 함. 블로커 발견 = 수정 + 재리뷰 후 보고.
4. **정직한 보고.** 한계를 있는 그대로 명시(예: "규칙은 X 강제 불가 — 트랜잭션이 authoritative", "기준 없음 = 대조 불가"). 가짜 대조 금지, trade-off 은폐 금지. 범위 밖이어도 발견한 기존 버그는 follow-up으로 플래그.
5. **검증 = 주장이 아니라 증거.** before/after 값, tight-loop 유니크 테스트, 재시도 드릴, 더블클릭 테스트, rules-simulator 매트릭스(admin/own-store/office/forged), grep 증명(예: stock write 0건), byte-unchanged diff. **모든 보고서에 검증 TABLE 포함.**

## 1.6 🔒 MONEY/STOCK-CRITICAL 패턴 (하우스 불변식 — 재사용, 재발명 금지)
- **Anchor 원칙**: 신규 기능은 기존 검증 프리미티브를 건드리지 말고 **폴딩(fold-in)**. 예: 모든 할인 타입 = per-line `unitDiscount` 로 해소(3c/3e byte-unchanged, 5-1→5-4); 모든 재고 이동 = storeInbound 원장 doc(moveType/stockDelta/defectDelta). 신규 설계 시 먼저 물을 것: "어떤 기존 프리미티브가 이걸 흡수하나?"
- **Exactly-once writes**: 결정적 doc id(= 업무번호: saleNo/adjNo/OUT-no/roNo) + update-denied(또는 field-restricted) 규칙 ⇒ 재시도의 재-set이 update로 평가 ⇒ 배치 전체 거부 ⇒ 이중반영 없음. pending 번호를 draft에 영속(pendingSaleNo 패턴); permission-denied 시 server read로 판별(exists = 이미 착지 → 성공 처리).
- **Atomicity**: 논리적 세션 1개 = 배치 1개. 상쇄 세션은 **절대 청크 금지**(부분반영이 손상). 상한 초과 → "나눠 진행" 메시지로 차단. 청크는 절대-SET origin(예: baseline 스냅샷)에서만 안전 — 차이를 알 것.
- **누적 상한(race)**(부분취소 패턴): 열거가능 결정적 id(`{base}_{seq}`)를 runTransaction 내 tx.get으로 read(트랜잭션 내 쿼리 불가); Firestore는 미존재-doc read도 충돌 추적 ⇒ 동시 초과반영 abort. 모호-재시도 멱등성=clientToken.
- **부호/듀얼-델타 역전**: 취소는 `increment(−stockDelta)` + `increment(−defectDelta)` 제네릭 역전(레거시 폴백 −qty). 전 이동타입 취소 로직 1개.
- **KST 산술만**: `_kstParts/kstDateKey/kstStamp/kstFormat`(UTC+9 산술). Intl/Asia-Seoul/로컬시간 경로 절대 금지. instant는 UTC ISO 저장; dateKey/번호/표시는 KST 유틸로 파생.
- **정수 KRW만.** % → 유닛별 floor; 분배액 → 비례 floor + 잔액은 최고가 라인. 합계는 **정확**해야 함(시뮬레이션으로 검증).
- **규칙 공예**: OR-union(most-specific-wins 아님); office-staff `!= ''` 가드; 레거시 doc은 `Map.get(field, default)`; field-restricted update = `diff().affectedKeys().hasOnly([...])` + 상태전이 조건; 규칙은 배열 내부/cross-doc 합 검증 불가(그곳은 트랜잭션이 authoritative — 명시할 것).
- **레거시 호환**: 스키마 확장은 폴백 헬퍼 접근자(`_mv*` 패턴)로 읽어 구 doc이 byte-identical 동작. 신규 필드는 additive; 구 레코드는 '-'/기본값 표시.
- **음수 재고 = 경고만, 차단 아님**(실물이 진실). 무료/번들 유닛도 재고 전량 **차감**(물리 반출; 판매가 0은 돈만 영향).

## 1.7 📤 보고 & GIT 규율
- 보고서: 영문, 리포트 마커(📋 작업 결과 / REPORT START…END), 섹션에 검증 table + 결정 로그 + 한글 테스트 체크리스트 + 정확한 배포 명령 포함. **청크로 붙여넣기**(소유주 클라이언트가 긴 붙여넣기 truncate; 표는 스크린샷이 효과적).
- 배포 명령은 변경 표면별 정확히: hosting-only vs `--only firestore:rules,...` vs `--only firestore:indexes,...`; 인덱스 빌드 시간 명시; 하드 새로고침 명시.
- Git: 선택적 스테이징(`git add js/ index.html style.css firebase.json firestore.rules firestore.indexes.json docs/ CLAUDE.md`; harness .claude/.firebase 제외). **완료 작업마다 커밋** — 여러 shipped 기능을 커밋 없이 누적 금지. worktree 브랜치 작업 시: 보고서에 브랜치+커밋 명시 + main-merge가 배포 선행임을 REMIND.
- CLAUDE.md는 매 작업 갱신(claude-md-history skill), 컴팩트 유지(규칙 verbatim, 이력은 1줄 changelog).

## 1.8 🧭 작업지시 계약 (WORK-ORDER CONTRACT — 결여 시 플래그)
적절한 작업지시는 다음을 명명한다: scope 경계(X ONLY), 리뷰어가 검증할 critical properties, STOP 조건, 배포 표면, 검증 체크리스트, 보고 형식. 들어온 지시에 critical-properties 목록 또는 scope 경계가 없으면, 명백한 것 이상으로 코딩하기 전에 (보고서로) **질문**할 것.

---

# 2. 아키텍처 불변식 & 데이터 모델 (Architecture Invariants & Data Models)

## 2.1 POS 재고 쓰기 3원칙 (비협상)
1. **`FieldValue.increment`만** 사용 — read-modify-write `+=` 절대 금지(동시성 유실).
2. **`set({merge:true})`** 사용, `update()` 금지 — 문서 없으면 throw. merge+increment가 문서/필드를 0부터 자동 생성.
3. **모든 write에 `storeId` 포함** — Firestore 규칙 + 쿼리가 의존.

## 2.2 POS 아키텍처 불변식
- **storeStock = 별도 per-doc 컬렉션** `storeStock/{storeId}_{productCode}` — 기존 `sharedData/products_*` 청크 모델 아님. 이유: 판매마다 전체 카탈로그 재다운로드 방지 + 원자적 차감.
- **`sizeLocations`는 `sizes`와 별개 top-level 키** — 위치=overwrite(setStoreStockLocation), 재고=increment(writeStoreStock). **math에 절대 안 섞임.**
- **`defectSizes`(6c 불량재고)도 `sizes`와 별개 top-level 키** — 🔴 **판매/정상 math 에 절대 안 섞임**(판매불가). 정상=`getStoreStock`(sizes), 불량=`getStoreDefect`(defectSizes). 판매확정/void 는 `sizes`만 increment → 불량 판매 구조적 불가. 전환(정상↔불량)=한 storeStock merge-set 에서 `sizes∓`/`defectSizes±` 동시 increment(원자).
- **append-only 원장**: `storeSales`·`storeInbound` = `update/delete: if false`. 예외: storeInbound는 **취소 전용 field-restricted update**(2.5 참조). storeSales **취소=새 레코드(역기록)** — 돈 장부는 불변(inbound-cancel의 update 방식과 의도적 divergence).
- **복합 ID** `{storeId}_{productCode}` — 품번 언더스코어 0건 확인 → 첫 `_` 분리 안전.
- **SIZES = `['XS','S','M','L','XL','2XL','F']`** (설계문서의 'XXL'은 오류). `SIZE_SPEC_SIZES`의 XXL은 사이즈규격 **측정용** 별개 상수(재고 무관).
- **정수 KRW 전용** — 라인 int×int, 파생값 항상 재계산(호출 totals 불신).
- **음수 재고 = 경고만**(차단 아님, 이미 판매 가능성, 1f 빨강 표시). **오프라인 확정 차단**(팬텀 큐 방지). **double-confirm 가드**(in-flight flag를 try 첫 줄 동기 set + finally 항상 release).
- **입고 등록 대상 = 바코드 등록된 (품번, 사이즈)만.** 바코드 추가 시 조회 재검색/재오픈 시 즉시 반영.
- **바코드**: 13자리(`/^\d{13}$/`), 엑셀 파싱 `raw:false`(텍스트 서식 보존). 앞자리 0은 엑셀 숫자 저장 시점에 파일 레벨 손실 → 복구 불가(경고만).

## 2.3 Firestore 규칙 지식 (비협상)
- **규칙은 OR-합집합** — "가장 구체적 규칙 우선" 아님. 매칭되는 어떤 allow든 true면 허용. 특정 문서 제한하려면 **일반 규칙에서 `docId != 'x'` 배제 + 구체 규칙 추가**.
- **office/미배정 staff `storeId != ''` 가드** — 빈값 `'' == ''` write 뚫림 방지.
- **미존재 필드는 `resource.data.get('field', default)`** — dot-access는 에러→거부(기존 문서에 필드 없으면 100% 거부).
- 헬퍼: `isAuth()` / `isApproved()`(status=='approved') / `isAdmin()`(grade≥3) / `userDoc`. **`isApproved()` 게이트**: `users` 본인 self-read 제외 전 컬렉션이 승인자만 통과(pending/rejected/suspended 차단). users self-read는 미승인도 허용(`checkApproval()`이 본인 doc 읽어야 함).
- **회원 self-update**: `grade`/`status` 불변(본인 승격 불가), `isAdmin`은 전 필드 가능.
- **users create**: `grade==1 && status=='pending'` 강제(권한상승 차단). 부작용: 첫 사용자 자동셋업(grade4) 차단 — 새 환경은 콘솔에서 첫 관리자 doc 수동 시드.
- **personalSchedules update/delete**: `createdBy == uid` 전용(관리자 override 없음).
- **삭제 규칙**: `comments`=작성자 OR admin, `leaves`=본인 OR admin, `users`=admin.
- **복합 인덱스**: `storeStock`/`storeInbound`/`storeSales` 각 (storeId ASC, dateKey ASC). storeInbound/storeSales 기간조회는 equality+range라 이 단일 복합인덱스로 서빙(orderBy 미사용, 클라 정렬).
- **storeInbound 취소 update**: admin OR 본인매장 + `get('cancelled',false)!=true` + `cancelled==true` + `cancelReason is string && size()>0` + `cancelledBy==auth.uid` + `affectedKeys.hasOnly([취소 5필드])`. delete=`if false`.
- **storeSales void (부분취소)**: 각 취소=회차별 doc `void_{saleId}_{NNNN}`(seq=1..N 연속). 🔴 **누적 상한(라인별 취소≤판매)은 runTransaction 이 authoritative** — 판매 doc + 그 판매 취소 doc들을 결정적 seq id 로 순회 read(웹 SDK 는 트랜잭션 내 쿼리 불가 → 결정적 id 열거로 회피, seq 상한=판매수량+2) → 누적 계산 → 초과 시 abort. 동시 취소는 같은 seq 를 노려 write→read-conflict 재실행→재검증 → over-void 불가. **멱등**: `clientToken`(회차 단위, 모호 재시도 시 같은 토큰 취소 있으면 no-op → 중복 복원 방지). 재고 복원=`increment(+voidQty)`(sizeLocations 불변), 트랜잭션 원자. 레거시 전량 `void_{saleId}`(구 3e)도 트랜잭션이 함께 read. create 규칙: `type∈['sale','void']`, void 는 `voidReason` 비어있지않음 + `voidedBy==uid`(id-agnostic → seq id 그대로 통과, **규칙 변경 불요**). 검출=`where('originalSaleId','in',[…])`(단일필드 auto 인덱스). 상태: 정상/일부취소/전체취소. 요약=Σ(판매−취소)=남은 실현매출(부분취소 netting).
- **sharedData/{key} 단일 문서 JSON 직렬화** (products/plan/events/work/settings/stores/inboundTypes 등) → per-row 권한 규칙 강제 불가, 클라 검증만(`canDeleteProduct`/`canDeletePlanItem`/`canDeleteEvent`/`canDeleteWork`). ~798개 상품이 단일 문서 976KB(1MB의 93%) — 청크/컬렉션 전환 향후 검토.
- **Storage 규칙**: `board/{postId}/`·`plan/{planNo}/`·`backups/{type}/` = `request.auth != null`(50MB). cross-service `firestore.get()` grade 체크는 IAM 권한 추가 필요→미설정 시 403 → JS 레이어(`_backupHasPermission` 등)로 admin 게이트.

## 2.4 POS 핵심 프리미티브 (core.js / store.js, window 노출)
- `resolveActiveStore()` — 관리자→`_storeViewOverride || 첫활성`, staff→`_currentUserStoreId || null`. self-heal: override가 활성목록에 없으면 리셋. (`_currentUserStoreId`는 로그인 시 캐시, 로그아웃 시 리셋.)
- `findByBarcode(bc)`→`{productCode,size}` (buildBarcodeIndex, O(1) Map, soft-deleted 제외). `getBarcodeCollisions()`.
- `storeStockDocId(storeId,code)` / `writeStoreStock(storeId,code,size,delta)`(merge+increment) / `setStoreStockLocation(storeId,code,size,loc)`(merge overwrite) / `loadStoreStock(storeId)`(server+cache fallback) / `buildStoreStockIndex(storeId)`(재고 `_storeStockIndex` + 위치 `_storeLocIndex` + **불량 `_storeDefectIndex`(6c)** 셋 다) / `getStoreStock(storeId,code)`(0맵 폴백, **sizes only — 판매 격리**) / `getStoreDefect(storeId,code)`(6c 불량 0맵 폴백, 정상과 별개) / `getStoreStockLocation(storeId,code,size)`.
- `normalizeLocation(loc)` — trim + 내부공백 제거 + 영문 대문자화(한글/숫자/하이픈 보존). choke point: 스캔 커밋·인라인 편집·확정 배치.
- **🕐 KST 표준 시각 유틸 (단일 소스, `store.js`)**: `kstDateKey()`(YYYY-MM-DD, 🔴 매출 귀속일) / `kstStamp()`(YYYYMMDD-HHMMSS) / `kstFormat(iso,fmt)`(저장 instant→KST 표시). **UTC+9 산술 고정**(`Date.getTime()+9h` → `getUTC*`) — **Intl timeZone/ICU/로컬 TZ 에 절대 비의존**. ⚠️ 모든 POS 시각/dateKey/번호/표시는 이 유틸만 사용(구 `Intl('Asia/Seoul')` + 로컬 catch 폴백은 ICU 없는 런타임·UTC 기기에서 UTC/로컬로 새어 saleNo·dateKey 가 ~9h 밀리는 버그 → 07-03 제거). `_inbDateKeyKST`/`_inbInboundNo`/`_inbHistDateTime` = 이 유틸 위임(이름 유지). soldAt/voidedAt/confirmedAt/updatedAt = `toISOString()`(UTC instant, 정렬·불변 안전) → 표시는 항상 kstFormat. 전제: 기기 UTC 시계 정확(물리 시계 틀리면 SW 교정 불가).
- 판매 원장 생성자: `generateSaleNo()`(SL-YYYYMMDD-HHMMSS**-<ms3+세션카운터2+랜덤3>** KST via kstStamp, 8자 접미사 — 판매 doc id 이므로 동일-초 다중 단말 충돌=판매 유실 방지: ms(1/1000)+단조 카운터(같은 ms 단말 내 유일)+랜덤 base36(46,656 교차단말). now 1회에서 초·ms 일관. 구 2자(uid파생1+랜덤1=같은 계정 36조합) 유실 버그 07-03 수정) / `buildSaleDoc()`(totals 재계산·정수·payMethod 화이트리스트) / `buildVoidDoc()` / `voidDocId()`(결정적) / `normalizePhone()`(숫자만) / `maskPhone()`(010-****-5678).
- `refreshAllProductViews()` — 3개 filtered(`[...allProducts]`) 재구축 + product/stock/sales/dashboard 렌더. 상품 생성/수정/삭제/복원 후 호출(sales.filtered 누락 방지).
- `_reNarrowFiltered(tab)` — `searchCriteria ? _narrowX(criteria) : [...allProducts]`. 검색 영속화(편집/동기화로 `.filtered` 재구축돼도 활성 검색 유지).

## 2.5 데이터 모델 (POS 컬렉션)
- **storeStock doc**: `{ storeId, productCode, sizes:{XS..F:qty}, defectSizes:{XS..F:qty}(6c 불량, absent=0), sizeLocations:{size:라벨}, updatedAt }` — **`sizes`(정상·판매가능)와 `defectSizes`(불량·판매불가)는 별개 버킷, 절대 합산 안 함**. 판매/정상 math=`getStoreStock`(sizes only), 불량 조회=`getStoreDefect`(6c). 규칙 field-agnostic(무규칙).
- **storeInbound doc (라인당 1) = 이동 원장(6a~)**: `{ storeId, productCode, size, qty(magnitude), inboundNo(=이동번호 IN-/ADJ-…), inboundType(라벨 스냅샷/재고수정), location, memo, workerUid, workerName, confirmedAt, dateKey(KST), batchId }` + **6a/6b/6c/6d 확장** `{ moveType:'inbound'|'adjust'|'outbound'|'defect-in'|'defect-out'|'defect-outbound'|'baseline'|'location-move', stockDelta(±, 정상재고 변동 truth — 반출=-qty · baseline=절대 origin), defectDelta(±, 불량재고 변동 — 6c) }` + **reason**(재고수정/불량전환 필수). **위치이동 doc(07-08)**: `moveType:'location-move'`, `stockDelta:0`/`defectDelta:0`(🔴 재고 무변경), `fromLocation`/`toLocation`, id=`{LOC-…}_{NNNN}` **결정적**(exactly-once), inboundType='위치이동'. storeStock write=**sizeLocations-only**(sizes/defectSizes 키 없음 → 수량 변경 구조적 불가) · normalizeLocation 적용 · **취소 불가**(입출고 내역·requestInbCancel 게이트 — 일반 취소는 옛 위치 미복원=반쪽 취소 → 되돌리기=반대 재이동) · 6d 대조 0 기여. **반출 doc(6b)**: `moveType:'outbound'`, `stockDelta:-qty`, `inboundNo=OUT-…`, id=`{OUT-…}_{NNNN}` **결정적**(exactly-once), inboundType 없음(표시는 moveType='반출'), `sizeLocations` 안 씀. **불량 이동 doc(6c)**: 전환 `defect-in`(sizes−N/defect+N)·`defect-out`(sizes+N/defect−N)=ADJ 결정적 id·듀얼 델타·사유 필수 · 불량반출 `defect-outbound`(stockDelta:0/defectDelta:-qty)=OUT-. **취소**=stockDelta·defectDelta **둘 다** 부호반전(6a 트랜잭션이 이미 처리). **기준 doc(6d)**: `moveType:'baseline'`, `stockDelta`=현재 정상재고(절대 origin)·`defectDelta`=현재 불량, id=`{BASE-…}_{code}_{size}`, inboundType='기준'. 관리자 전용·재실행=새 BASE 대체(최신 기준만 대조). **입출고 내역(2d)에서 제외**(품목 원장 전용). 취소 시 `{ cancelled:true, cancelReason, cancelledBy, cancelledByName, cancelledAt }`. ⚠️ **레거시 doc(6a 이전)엔 moveType/stockDelta 없음** → 읽기 backfill(moveType→inbound, stockDelta→+qty, defectDelta→0; `_mvType`/`_mvSignedQty` 등). 재고수정 doc id=`{adjNo}_{NNNN}` **결정적**(exactly-once), 입고 doc=auto-id.
- **storeSales sale doc**: `{ type:'sale', saleNo(SL-...), storeId, lines[{productCode,size,qty,unitPrice,unitDiscount,lineNormal,lineDiscount,lineTotal,discountSource, appliedRuleId?,appliedRuleName?(5-1 스냅샷), cartDiscount?,cartRuleId?,cartRuleName?(5-2 카트몫·per-unit), lineId?,lineNote?(5-3 split 식별·무료/반값/콤보/번들 라벨), bundleInstanceId?,cbDiscount?(5-4 번들 인스턴스·콤보/번들 몫) — 전부 additive}], totals{total,discountTotal,qtyTotal}, appliedDiscounts:[{ruleId,name,level('line'|'cart'),saving,qty}](분석용), payMethod, customerPhone, workerUid, workerName, soldAt, dateKey }` — **discountSource: 'manual' | 'store-discount'(자동) | 'manual-override'(수동 잠금)**. 판매가=정상가−할인가 불변(모든 할인=unitDiscount 만 채움 → 3c/3e/부분취소 math 무변경). 🔴 **5-3 split**: 부모 라인이 정상/무료/반값 서브라인으로 분할될 때 `lineId`(`{code}#{size}#{n}`) 로 라인 독립 식별 — 부분취소/집계/취소UI/누적상한이 `_lineIdOf(l)=lineId||(code,size)` 로 키잉(레거시/비-split=byte-identical). **무료 유닛(unitDiscount=unitPrice, 판매가0)도 재고 차감**(물리적 반출).
- **storeSales void doc**: `{ type:'void', originalSaleId, originalSaleNo, storeId, lines, totals, voidReason, voidedBy, voidedByName, voidedAt, dateKey, customerPhone }`
  - customerPhone: 공홈 적립금 대사 전용(회원관리 아님). 선택 입력, 숫자만 저장('010-1234-5678'→'01012345678'), 일반 이력=마스킹, 폰검색=전체(equality→단일필드 auto 인덱스).
- **replenishOrders doc (R1, 발주=요청 문서)**: `replenishOrders/{roNo}` — `{ roNo(RO-YYYYMMDD-HHMMSS-<8자>), storeId, storeName(스냅샷), lines:[{productCode, productName(스냅샷), size, requestQty(int≥1)}], totals{lineCount,qtyTotal}, memo, status('requested'|'cancelled'), confirmChecked/By/At(R2 확인✓ 예약), shipChecked/By/At(R2 발송✓ 예약), cancelledBy/At/Reason(취소 시), createdByUid/Name, createdAt(UTC ISO), dateKey(KST) }` — 🔴 **storeStock/storeInbound/storeSales 절대 무접촉**(요청 원장). doc id=roNo 결정적 → 재제출 set=update 평가→규칙 거부→**중복발주 불가(exactly-once)**. **checks=false→true 단방향**(R2 진행 마커, 되돌리기 없음). **요청중(pending) 정의=status'requested'&&shipChecked==false** 합(발송되면 이탈=lifecycle). **인덱스**: (storeId,dateKey)=내 발주 조회 · (status,dateKey)=R2 크로스매장 예약. append-only(delete:false), 취소/체크만 field-restricted update.
- **_stores config**: `[{id(st1,st2..), name, active, order, location}]` — localStorage `lemango_stores_v1` + `sharedData/stores`(admin write). 기본 부산점(st1)/성남점(st2). `generateStoreId`=soft-disabled 포함 최대 접미사+1. 삭제 가드 `storeHasData`(재고/매출 있으면 hard-delete 거부→비활성화). `getActiveStores()`.
- **_inboundTypes config**: `[{id, name, active, order}]` — localStorage `lemango_inbound_types_v1` + `sharedData/inboundTypes`(admin write). DEFAULT 신규입고/조정입고/이관입고. `getActiveInboundTypes`/`inboundTypeHasData`. **"입고취소"는 유형 아님 — 별도 cancelled 상태.**
- **_storeDiscounts config (5-1~5-4, ✅ Phase 5 완료)**: `[{id(sd1..), name, active, condition, benefit, period{start,end}(KST·빈값=상시), storeScope('all'|storeId), priority, order}]` — localStorage `lemango_store_discounts_v1` + `sharedData/storeDiscounts`(admin write, OR-union). `getActiveDiscounts`(활성+기간+매장, **condition-agnostic**). **condition**: `product{productCode}`(5-1) · `brand{brand}`·`category{category}`(5-2 상품 brand/**type**) · `cartTotal{minTotal}`(5-2 카트) · `qty{scope,ref}`(5-3 수량) · `combo{items[{scope,ref}],eachQty}`·`bundle{items,eachQty}`(5-4 조합). **benefit**: `percent{value}`·`fixed{price}`(라인) · `amount{minus}`(카트/콤보) · `nplus{buy,free}`·`secondHalf{nth,value}`(5-3) · `bundlePrice{price}`(5-4 고정가). 엔진 `_saleApplyDiscounts`=**파이프라인 line→quantity→combo/bundle→cart**: pass1 라인·**pass1.5 수량**(`_saleApplyQtyDiscounts`)·**pass1.7 콤보/번들**(`_saleApplyComboBundle`: 미claimed 유닛으로 인스턴스 형성=멤버별 floor(avail/eachQty) 최저가 예약, combo=%/정액·bundle=S−price 정확 분배, 부모 `_bundleGroups`)·effective 라인 빌드·pass2 카트(콤보/번들 라인 제외). 🔴 **effective(split) 라인**(`_saleEffLines`): 부모→정상/반값/무료/콤보/번들 서브라인(각 균일 unitDiscount), split 시 **lineId**(`{code}#{size}#{n}`). 🔴 **모든 할인=unitDiscount 폴딩(별도 머니필드 없음)→3c/buildSaleDoc/부분취소 byte-unchanged**. 🔴 **무료/번들 유닛도 재고 차감**(확정=effective code/size별 qty 합산=전체). 🔴 **번들=whole-instance-void-only**(bundleInstanceId 태그; 취소모달이 번들 멤버 부분취소 차단·"번들 전체 취소" 체크박스만→같은 instanceId 전 라인 남은전량 원자 취소; 트랜잭션은 무변경=UI 제약). **콤보(%)=부분취소 per-unit 스냅샷 허용**(고정가 아님). cartDiscount/cbDiscount/lineId/lineNote/bundleInstanceId=표시·귀속 전용 additive. 라인+수량+콤보/번들 across-level(콤보/번들 유닛은 카트 제외), 각 레벨 no-stack, manual-override 전부 제외. 스냅샷=취소 시 재계산 없음. **관리 UI=`🏬 매장→'매장 할인 상품 관리'` 서브탭**(조회 전 직원·관리 grade≥4; 콤보/번들 멤버=`scope:ref` 콤마 입력). 규칙/인덱스 무변경.

## 2.6 데이터 모델 (기존 앱)
### 상품 (`State.allProducts` 아이템)
```js
{ no, brand, productCode, sampleNo, cafe24Code, barcode,
  nameKr, nameEn, colorKr, colorEn, colorCode, salePrice, costPrice,
  type, backStyle, legCut, guide, fabricType,
  chestLine, transparency, lining, capRing, material, comment, washMethod,
  sizeSpec,        // size-first 중첩 객체 (2.7 참조)
  modelSize, madeMonth, madeBy, madeIn, videoUrl,
  saleStatus('판매중'|'종료'|'추가생산'), productionStatus('지속생산'|'생산중단'),
  productCodeLocked, assignee/assigneeName/assigneePosition, pinnedMemo,
  deleted, deletedAt, deletedBy, deletedByName,   // soft-delete (휴지통)
  images:{ sum, lemango, noir, external, design, shoot },
  tempImages:[{url,type,name,path?,fromPlan?}],   // 참고 이미지
  barcodes:{ XS..F },  stock:{ XS..F },
  stockLog:[{type:'in'|'out', date, XS..F, memo, registeredAt}],
  sales:{ 공홈, GS, ... },   // 키는 _platforms 동적
  scheduleLog:[{confirmedAt, schedule:{design,production,image,register,logistics}}],
  registDate, logisticsDate,
  revenueLog:[{type:'sale'|'refund', date, channel, orderNo, qty, revenue, registeredAt}],
  createdBy/createdByName/createdAt, lastModifiedBy/lastModifiedByName/lastModifiedAt,
  confirmedBy/confirmedByName/confirmedAt }   // 07-07: 기획→상품 확정자 기록(추적성). 상품 + 소스 기획항목 양쪽 스탬프(`_stampConfirmedBy`, UTC ISO). 단일·일괄 확정 공통.
```
- 기본 정렬: **registDate desc(최신 등록순)** — product/stock/sales 3뷰 공통. 렌더 시 `State.X.sort`로 정렬(단일 권위). cross-reload 정렬 영속화는 의도적 제거.

### 플랜 (`State.planItems`) — 상품 필드와 동일화 + `year, season, gender, memo`, `schedule:{design/production/image/register/logistics:{start,end}}`, `confirmed`, `tempImages`. localStorage `lemango_plan_items_v1` + Firestore `sharedData/planItems`(단일 문서, ~300건에서 1MB 접근). `savePlanItems()`/`loadPlanItems()`.

### 업무일정 (`State.workItems`) — `{no, category, title, startDate, endDate, memo, useVehicle, checklist:[{id,text,done}], kanbanStatus, registeredAt, createdBy}`. localStorage `lemango_work_items_v1`.

### users (Firestore) — `{ uid, email, name, phone, dept, position, grade(1~4), storeId, status('pending'|'approved'|'rejected'|'suspended'), createdAt, lastLogin }`
- **등급 4단계**: 4=시스템 관리자 / 3=관리자 / 2=부서장 / 1=담당자. (GRADE_DEFS엔 grade5(대표이사)도 있으나 KPI 외 미사용.)
- 신규 가입: `grade:1, status:'pending'` → 관리자 승인 필요.

### personalSchedules (Firestore) — `{ title, startDate, endDate, category, memo, mentions:[{type:'user'|'dept',uid?,name?,dept?}], createdBy/createdByName/createdByDept, createdAt }`. 가시성: grade≥4=전체, 본인/멘션유저/멘션부서=O. 수정/삭제=본인만(2.3).

### posts (게시판, Firestore) — `{ boardType('notice'|'free'), category, title, content, authorUid/Name/Grade/Position, pinned, important, views, commentCount, attachments:[{name,size,url,path}], createdAt, updatedAt }`. 첨부=Storage `board/{postId}/`(base64→Storage 이관 완료).

### comments (Firestore) — `{ modalType('product'|'plan'|'event'|'work'|'member'|'board'), targetId, uid, userName, userGrade, userPosition, content, createdAt, updatedAt }`. 수정/삭제=작성자 OR grade≥3.

### activityLogs (Firestore) — `{ timestamp, uid, userName, userPosition, userDept, action, target, detail, ip }`. `logActivity()` fire-and-forget + localStorage mirror `lemango_recent_activity_v1`(500 FIFO). action: login/logout/create/update/delete/upload/approve/setting/error/inbound/inbound-cancel/adjust/outbound/baseline.

## 2.7 사이즈 규격 단일 소스 (SIZE_SPEC_PARTS)
- **측정 부위 추가 = `SIZE_SPEC_PARTS`(core.js)에 1줄 append만으로 화면·엑셀·HTML 전체 자동 반영** — Phase A 리팩터 성과.
- 데이터: **size-first 중첩** `sizeSpec = {XS:{bust,waist,...}, S:{...}, ..., F}`.
- `SIZE_SPEC_SIZES = ['XS','S','M','L','XL','XXL']` + F. `SIZE_SPEC_PARTS` = 15개 부위: 가슴/허리/엉덩이/총장/어깨/소매/밑단/총장(상)/총장(하)/밑가슴/허벅지단면/컵가로/컵세로/앞허리/뒤허리.
- `buildSizeSpecColumns()` = 6사이즈×15부위+F = **91 엑셀 컬럼**. `console.assert`로 드리프트 감지. 엑셀 컬럼 생성은 **반드시 이 함수로만**(하드코딩 금지).
- 헬퍼(utils.js): `buildSizeSpecView`/`buildSizeSpecEdit`/`collectSizeSpec` (모두 `SIZE_SPEC_PARTS` 루프). `getActiveParts(sizeSpec, activeSizes)`(core.js) = 값 있는 부위만 반환 → 보기 테이블 + `copySizeGuideHtml`(HTML 복사)이 **빈 측정항목 제외**(filled-only). 수정모드는 항상 15종 표시.
- 부위 추가는 가산적(마이그레이션 불필요, 기존 데이터는 신규 부위 빈값→보기/HTML 자동 숨김). F 단일값 특수처리 유지.

## 2.8 품번 자동생성 규칙
- 형식: `[분류2][성별1][타입2][디자인4][연도1][시즌1][일련번호2]` = 13자리. **prefix=11자**(분류2+성별1+타입2+디자인4+연도1+시즌1).
- 일련번호: 같은 prefix 내 00~99 미사용 자동. canonical 패턴: `c.length === prefix.length + 2 && c.startsWith(prefix)` + `c.slice(-2)` (4 사이트 통일: 상품 등록/수정 + 기획 등록/수정).
- 중복 체크: `State.allProducts` + `State.planItems` + `_reservedCodes`(임시예약 Set, 취소/닫기 시 해제). 자기 코드 제외.
- 분류 dropdown은 `_classCodes` **라이브 읽기**(하드코딩 literal은 fallback만). 디자인번호는 `_designCodes` 단일 소스.
- **Plan은 sampleNo만 필수, productCode 선택** — 단 `confirmPlanToProduct()`(상품 이전) 시점엔 품번 필수(매칭 키).

---

# 3. 현재 상태 & 로드맵 (Current Status & Roadmap)

## 3.1 완료
- **Phase 1 ✅** 매장 기반: 1a 매장 config → 1b 사용자 storeId + `resolveActiveStore()` → 1c 매장 탭 shell + 관리자 스위처 → 1d storeStock 모델 + Firestore 규칙 → 1e 재고 엑셀 업로드(SET/ADD) → 1f 매장별 재고현황 뷰 + 상세 모달.
- **Phase 2 ✅** 입고 스캔: 2a 고정 로케이션(sizeLocations) → 2b 스캔 화면(커서 상태머신 + 4대 차단규칙 + staging + draft) → 2b-r 허브 창 재하우징 + UX 재설계 + 품번조회 → 2c 최종 확정 원자적 반영(storeStock increment + sizeLocations + storeInbound 이력, double-confirm) → 2d 입고 내역 뷰. + 업그레이드(기간조회/입고번호/메모/엑셀) + 입고 취소(runTransaction 역반영 + 사유 필수) + 입고 물류 강화(입고유형/유형설정/유형·상태필터/로케이션 정규화).
- **Phase 3 ✅ 완료** (scan→sale→deduction→history→void): 3a 매출 원장 데이터층 + 규칙 refinement + 복합인덱스 · 3b 판매 화면 + 공용 상품 상세 모달 · **3c 판매 확정 라이브(원자적 재고 차감 + 매출 원장 batch, exactly-once)** · **3d 매출 조회(기간/번호 · 필터 · 영수증 · 엑셀 · 취소상태 정합성 존재조회, 읽기전용)** · **3e 판매취소 라이브(재고 복원 increment(+qty) + append-only 역기록 void_{saleId}, double-void 구조적 불가) + 매출 조회 전용 서브탭 승격**.
- **Phase 6 ✅ 완료** (물류 원장 확장): 6a 재고수정(실사)+이동 원장 앵커(moveType/stockDelta) · 6b 반출(방향 토글, 음수 movement, OUT-) · 6c 불량재고(defectSizes 병렬 버킷, 정상↔불량 전환, 불량반출, 판매 격리) · **6d 품목 이동 원장 통합뷰 + 기준재고(baseline) + 장부↔실재고 대조(정직)**. 남은 6e(판매 미러)=옵션.

## 3.2 다음 = (선택) POS Phase 6e 판매 미러 · 강제 재고 차감 · Phase 4 환불
**✅ Phase 6 완료** (6a~6d, 설계 `docs/pos-phase6-design.md`, 오픈질문 8개 모두 "추천대로" 확정). 남은 6e(판매 미러=전기간 정확 대조)는 **옵션**(현재 MVP 기간 스캔으로 충분). **6d ✅ 품목 이동 원장 + 기준재고(baseline) + 대조(reconciliation)**: 품번+기간별 **통합 타임라인**(이동=storeInbound 품번인덱스[6a] + 판매/취소=storeSales 기간스캔[3a] 클라 라인필터 — embedded lines 라 품번 쿼리 불가, Q5 MVP) → 병합·시각 ASC·**누계**(기준 이후 절대치, baseline=리셋·취소이동=제외)·**장부↔실재고 대조**(기준 있고 기간이 오늘까지 커버할 때만; 없으면 "기준 미설정—대조 불가" 정직 표시, 가짜 일치 없음; 불일치=장부X/실Y 양수 표기). **baseline**(관리자 [🧭 기준 재고 설정])=비어있지 않은 (품번,사이즈)마다 `moveType:'baseline'` SET origin(stockDelta=현재 정상, defectDelta=현재 불량) doc `{BASE-…}_{code}_{size}`, 청크 450(SET origin이라 부분완료 무해·**재실행=새 BASE 번호로 대체**, 대조는 사이즈별 '최신' 기준만 → 이중계상 없음). 진입=재고현황 [📊 품목 원장](전 직원)·상세 모달 [📊 이동 원장]·baseline=grade≥3. baseline 이동은 **2d 입출고 내역에서 제외**(원장 전용). 엑셀+대조요약. logActivity('baseline','기준재고설정'). **판매 write 경로 무변경**(스캔은 읽기전용). **규칙/인덱스 변경 없음**(6a·3a 인덱스 재사용) → `--only hosting`. **6c ✅ 불량재고(Defect, scope C=전환+표시+불량반출)**: `storeStock` 에 **`defectSizes` 병렬 버킷**(판매불가) 추가 — `sizes`(정상)와 **절대 합산 안 함**. 🔴 **판매 격리(구조적)**: 판매/1f/입출고/재고수정 정상 math 는 `getStoreStock`(sizes only)만 읽음 · 신규 `getStoreDefect`(`_storeDefectIndex` 별개)만 불량 조회 · `loadStoreStock` 필드 화이트리스트에 defectSizes 추가한 곳이 유일 노출점 → 불량이 판매로 새지 않음(감사 확인). **정상↔불량 전환**=재고수정(6a) 창에 per-line **op 선택**(재고조정±/정상→불량/불량→정상): 전환=`sizes∓N`+`defectSizes±N` **한 merge-set 동시 increment(원자)** + 이동 doc `moveType:'defect-in'|'defect-out'` **듀얼 델타**(stockDelta/defectDelta) · 사유 필수 · ADJ 결정적 id · 단일 배치. **불량반출**=반출 스캔(6b)에 **3번째 방향** '불량반출'(defect-outbound): `defectSizes:increment(-qty)`(정상 sizes 무관) · moveType `defect-outbound` · OUT- · exactly-once. **표시(Q7)**: 1f 불량 배지(>0) + 상세 모달 사이즈별 정상/불량/로케이션 컬럼. **입출고 내역**: 불량증감 컬럼 + 유형 필터(정상→불량/불량→정상/불량반출) + 엑셀. **취소(Q8)**: 6a 취소 트랜잭션이 **두 델타 모두 역반영**(`increment(-stockDelta)`+`increment(-defectDelta)`) — 6a 부터 전방호환이라 무변경. **규칙/인덱스 변경 없음**(storeStock write 규칙 field-agnostic → defectSizes 무규칙 · 이동 doc=storeInbound create 규칙) → `--only hosting`. **6b ✅ 반출(Outbound)**: 입고 스캔 창에 **방향 토글(정상입고/정상반출)** 추가 — 반출=음수 movement(`moveType:'outbound'`, `stockDelta:-qty`), 번호 `OUT-…`. 스캔/staging/draft/entry 카드는 입고와 전면 재사용, **확정만 분기**: 결정적 doc id `{OUT-…}_{seq}` + `_pendingOutNo`(draft 동결) → 단일 원자 배치(청크 금지, >450 차단) storeStock `increment(-qty)` + 이동 doc → 재시도 시 update 규칙 거부 → **exactly-once(6a 미러, 재차감 불가)**. **방향별 draft 분리**(`lemango_inbound_draft_*` vs `lemango_outbound_draft_*`, 기존 입고 키 불변) + 전환 시 swap → +qty 라인이 -qty 로 재해석 불가. 로케이션=참고용(반출 시 `sizeLocations` 안 씀·빈값 허용). Q3 재고부족=경고+진행(staging 빨강 + 확인창 음수 열거). 입출고 내역=반출 유형 필터·음수 증감·OUT-·취소=부호반전(+qty 복원, 6a 제네릭 취소 그대로). logActivity('outbound','반출'). 정상입고 흐름 byte-unchanged. 규칙/인덱스 변경 없음(이동 doc=storeInbound create 규칙, 6a 인덱스 재사용) → `--only hosting`. **6a ✅ 재고수정(실사) + 이동 원장 앵커**: `storeInbound` 를 per-line **이동 원장**으로 확장(`moveType` + signed `stockDelta`/`defectDelta`; 레거시 doc=moveType 없음→inbound·stockDelta 없음→+qty). 재고수정 = 품번 조회(**전 사이즈**, 바코드 무관 — 실사) → ±증감 다라인(상쇄 세션 허용) → 사유 필수 → **단일 원자 배치**(청크 금지, >450 차단) storeStock `increment(±)` + 이동 doc `{adjNo}_{seq}` **결정적 id**. exactly-once: 재시도 시 기존 doc set=update → storeInbound update 규칙(취소 5필드만) 거부 → 배치 전체 거부(재반영 없음). Q4=+조정 로케이션없음 경고+진행. 2d 뷰→"입출고 내역"(moveType 유형/부호 증감/사유 표시 + 재고수정 필터), 취소=stockDelta 부호반전. 신규 인덱스 `storeInbound(storeId,productCode,dateKey)`(6d용). **규칙 변경 없음**(create/cancel 규칙 필드 화이트리스트 없음). 이후: **6b 반출**(입고스캔 방향토글, −stockDelta) → **6c 불량재고**(`defectSizes` 맵) → **6d 통합뷰+baseline** → **6e(옵션) 판매미러**.
> ✅ **3c**: 판매 확정 = 단일 batch(`storeStock` `increment(-qty)` 코드별 그룹 + `storeSales` sale doc, **id=판매번호 결정적**) → 규칙 `update:false` 재시도 시 배치 전체 거부 → **재차감 구조적 불가(exactly-once)**.
> ✅ **3d**: 매출 조회(읽기전용). 기간 쿼리 3a 복합인덱스 EXACT. 🔴 취소 정합성 = 취소 doc(`void_{saleId}`) **직접 존재조회**(`documentId() in` 30청크)로 판별 → 다른 날 취소도 '취소됨' 마킹·요약 제외. 번호검색 `customerPhone==`(전체번호=마스킹 유일 예외).
> ✅ **3e**: 판매취소 라이브 = 단일 batch(`storeStock` `increment(+qty)` 복원 코드별 그룹 + `storeSales` **void doc id=`void_{saleId}` 결정적**, buildVoidDoc) → 재시도 시 기존 void doc=update → 규칙 거부 → **double-void 구조적 불가**. 원본 판매 doc 불변(append-only). 사유 필수(클라+서버), 권한=관리자 OR 본인 매장(office/타매장 버튼 숨김), 3c급 프리플라이트/타임아웃/denied-착지 재확인, `sizeLocations` 불변. **매출 조회 = 전용 서브탭 승격**(모달→패널, 판매↔재고현황 사이, 전 직원). **규칙/인덱스 변경 없음 → `--only hosting`**(void create 규칙은 3a 선배포).

## 3.3 배포 관련
- 통상 변경: `firebase deploy --only hosting`. 규칙 변경 포함: `firebase deploy --only firestore:rules,hosting`. Storage: `firebase deploy --only storage`.
- **캐시 헤더**(firebase.json): `**/*.@(js|css)`·`/index.html`·`/`에 `Cache-Control: no-cache, max-age=0, must-revalidate`(ETag 재검증). 헤더는 **다음 배포부터 효력** — 배포 후 1회 하드 새로고침 필요.

---

# 4. 알려진 한계 / 보류 (Known Limitations / Deferred)
- **강제 재고 차감** — Phase 3 이후 (사유+로그 필수, 방침 확정).
- **이관(매장간)** — 입고유형에 '이관입고'만 있음, 실제 이관 흐름 미구현.
- **Phase 4 환불/부분환불**. (**로케이션 서브탭 07-08 제거** → 재고현황 검색[바코드 정확/품번 부분] + 공유 상세 모달로 대체.) **매장 할인**: 설계 `docs/pos-phase5-discount-design.md`(condition×benefit) + **✅ Phase 5 매장 할인 완료** — 5-1 상품별 %/특정가 · 5-2 카테고리/브랜드 + 총액(카트분배) · 5-3 N+1/두번째반값(수량, split-line+lineId, 최저가 무료) · **5-4 콤보/번들(조합, 인스턴스 형성, 번들 고정가 정확분배, whole-bundle-void-only)**. 전부 unitDiscount 폴딩 → 3c/부분취소 byte-unchanged.
- **보충 발주 R1+R2 ✅ 완료** — R1(발주 생성) + R2(물류 확인 뷰 + 확인✓/발송✓ 표시체크). **정식 마감(closure)/부분발송/거절/최소재고(min-stock)/이관연동=v2**(현재 발주=요청 문서, 물류 발송은 표시체크 후 별도 입고 스캔으로 물리 반영). ⚠️ **크로스탭 동시 발주 확정**=서로 다른 roNo 중복 doc 가능(순차 안전, request-only=재고 무영향, `_reservedCodes` 크로스탭 한계와 동류).
- **적립금 지급표시 / 거스름돈 계산 / 분할결제**(payMethod 단일값) — 추후 옵션.
- **activityLogs 무제한 증가** — retention(예: 90일) 정책 미도입.
- **레거시 Firestore 백업**(`backups/{type}/items/`) — 복원만, 신규는 Storage `backups/{type}/{date}.json`. 수동 정리 대기.
- **엑셀 업로드 엣지**: 소프트삭제 품번과 동일 품번 업로드 시 삭제 항목 조용히 업데이트 → 거부+경고 추가 검토.
- **cross-tab 품번 race** — `_reservedCodes`가 탭별 메모리 비공유.
- **단일 상품확정(confirmPlanToProduct)의 이중 write** — 일괄 확정(applyBulkConfirm)은 단일 batch 로 원자화됐으나, **단일 확정은 여전히 fire-and-forget** savePlanItems + saveProducts(오너 지시로 behavior 불변 유지). saveProducts 실패 시 orphan in-memory 상품(리로드 유실) 가능. 향후 단일 경로도 원자화 검토(일괄과 동일 batch 재사용).
- **Option E 색상 데이터 정규화**(1회, 수동) — 백업 검증 후 설정→🎨 색상 관리→🔍 마스터 매칭(`runColorMigration`)으로 ~798개 colorCode 백필 + colorEn canonical화. 긴급 아님(Option B diff가 이미 라운드트립 보호).
- **RC2 잔여**: `refreshAllProductViews`(로컬 편집)는 3개 테이블 무조건 렌더(기존 동작). POS 도입 시 `_fsLoadProducts` 전체 재로드 granularity가 병목 — 별도 아키텍처 검토.
- **undefined CSS vars** — cosmetic.
- **정품 확정 전 활성화 주의**: `dashboard.js` 매출 "전월"이 `Math.random()` 가짜 데이터(실데이터 결선 필요), 초기 관리자 비번 평문 하드코딩(auth.js).

---

# 5. 도메인 레퍼런스 (Domain Reference)

## 5.1 파일 구조
```
index.html · style.css · firebase.json · .firebaserc · firestore.rules · firestore.indexes.json · storage.rules
js/  core.js(State/설정/POS 프리미티브/populateAllSelects) · router.js(탭 바) · utils.js(유틸/페이지네이션/정렬필터/사이즈규격뷰)
     products.js · stock.js(재고·입고·출고·바코드업로드) · sales.js · gonghom.js(cafe24) · sabangnet.js · plan.js
     event.js · dashboard.js · modals.js · register.js · excel.js · settings.js · design.js · upload.js · work.js
     auth.js · comments.js · board.js · activity-log.js · members.js · hr.js · orgchart.js · color-master.js · trash.js
     backup.js · store.js(POS 매장 탭 전체) · product-code.js · main.js(init/initApp)
data/ products_lemango.json · products_noir.json · combined.json
docs/ pos-phase1~3-design.md · **pos-phase6-design.md**(재고수정/반출/불량/통합원장 심층설계) · **pos-phase5-discount-design.md**(매장 할인 condition×benefit 심층설계) · phase-a-report.md · permission-model.md · pre-activation-audit.md 등
```

## 5.2 탭 (권한: TAB_PERMISSIONS)
| 탭 | ID | 최소 grade | 비고 |
|----|----|:-:|------|
| 대시보드 | tab-dashboard | 1 | 공지/최근등록, 캘린더(행사/기획/업무/개인), 오늘의 팀 활동 |
| 상품조회 | tab-product | 1 | 검색/필터/정렬, 멀티선택, 인라인편집, 품번→상세모달 |
| 재고 관리 | tab-stock | 1 | 사이즈별 재고, 신규입고/출고/바코드 업로드 |
| 매출현황 | tab-sales | 1 | 플랫폼별 피벗, 판매 업로드(카페24/사방넷/면세점) |
| 신규기획 | tab-plan | 1 | 기획 관리, 일괄일정, 상품 이전, 복제, 삭제 |
| 🏬 매장 | tab-store | 1 | POS. 조회=전원, 작업=본인매장+관리자(권한방침). 서브탭: 판매/매출조회/재고현황(바코드·품번 검색 + 불량재고 목록)/할인/**물류 발주 확인(R2)** (로케이션 서브탭 07-08 제거) |
| 행사일정 | tab-event | 1 | 월/주/일 캘린더 |
| 업무일정 | tab-work | 1 | 업무/개인(이너탭) + 칸반 + 주간리포트 |
| 게시판 | tab-board | 1 | 공지/자유, 첨부(Storage) |
| 조직도 | tab-orgchart | 1 | 부서별 카드, 전화번호 |
| 인사관리 | tab-hradmin | 2 | 회원관리 병합 + 백업관리(grade4) |
| 🗑️ 휴지통 | tab-trash | 3 | 소프트삭제 상품 복원/영구삭제 |
| 설정 | tab-settings | 4 | 기본 옵션·매장·입고유형·색상·알림 관리 (매장할인 관리는 🏬 매장 서브탭으로 이전) |
- 이중 차단: nav 버튼 숨김(`updateTabVisibility`) + 라우터 가드(`canAccessTab` in openTab/navigateTo/hashchange/popstate). 미등록 탭=전체개방(신규 탭 추가 시 TAB_PERMISSIONS 필수 등록).

## 5.3 전역 상태 & 설정 변수
- **State**: `allProducts, planItems, workItems, boardPosts`, 탭별 `{filtered, page, pageSize, sort, columnFilters, searchCriteria, needsRerender, activeColumns, inactiveColumns}`, `currentUser, members, selectedProducts(Set), sales.activePlatforms/inactivePlatforms, workCalView/eventCalView, barcodeIndex`.
- **설정(localStorage + Firestore sharedData 이중)**: `_settings`(SETTING_DEFS 옵션) · `_channels`(판매채널 [{name,feeRate,note,active}]) · `_platforms`(활성 채널명, _channels서 동기화) · `_designCodes`([code,en,kr]) · `_classCodes` · `_depts` · `_events` · `_workCategories`/`_workItems` · `_stores` · `_inboundTypes` · `_storeDiscounts`(5-1 매장 할인 규칙) · `_colorMasters`(111색 10카테고리, color-master.js).
- **메모리 변수**: `_reservedCodes, _detailCode, _detailPendingCode, _editingPlanNo, _pdPendingCode, _planSelected, _personalSchedules, _allUsers, _currentUserName/Position/Dept/Grade/StoreId, _storeViewOverride, _editingPsId, _notifications, _watches, _storeStockIndex, _storeLocIndex, _inbList` 등.
- `POSITIONS`=['사원','주임','대리','과장','차장','실장','팀장','부장','이사','대표이사']. `DEFAULT_PLATFORMS`=['공홈','GS','29cm','W쇼핑','기타'].
- `populateAllSelects()` — _settings + _platforms + POSITIONS + 각 config 기반 모든 select 동적 채움.

## 5.4 이미지 우선순위
- `getThumbUrl()`: sum → lemango → noir → design → shoot. 폴백: `PLACEHOLDER_IMG = 'assets/logo-placeholder.png'`(SVG data URI 아님, assets 파일). 로고: `assets/logo-white.png`(다크), `logo-black.png`(라이트).

## 5.5 모달 목록 (핵심)
`imageModal, detailModal(openDetailModal, readOnly opt), registerModal, stockRegisterModal, outgoingModal, barcodeUploadModal, salesUploadModal(카페24/사방넷/면세점), gonghomPreviewModal, sabangnetPreviewModal, planDetailModal, planDeleteConfirmModal, productDeleteConfirmModal, eventRegisterModal, planScheduleModal, workRegisterModal, workDetailModal, weeklyReportModal, dashDayModal, dashInfoModal, signupModal, memberEditModal, memberAddModal, memberProfileModal, bulkScheduleModal, personalScheduleModal, activityDetailModal`.
POS: `storeStockUploadModal, storeStockDetailModal(공용 상품 상세 openStoreProductDetail), inboundScanModal(ESC 차단·명시적 닫기 · **6b/6c 방향 토글 정상입고/정상반출/불량반출**), inbLookupModal, inbCloseConfirmModal, inbHistoryModal(→입출고 내역), inbCancelModal, ledgerModal(6d 품목 이동 원장·대조), baselineConfirmModal(6d 기준재고), saleLookupModal, saleConfirmModal(3c 사전확인), saleReceiptModal(3d 영수증), saleVoidModal(3e 판매취소), adjustModal(6a 재고수정·ESC 차단·명시적 닫기 · **6c op 정상↔불량 전환**), adjConfirmModal(6a 사유), adjCloseConfirmModal(6a 닫기확인), locMoveModal(위치 이동·ESC 차단·명시적 닫기·재고 무변경), locConfirmModal(위치 이동 확정), locCloseConfirmModal(위치 이동 닫기확인), replenishModal(R1 보충 발주 창·ESC 차단·명시적 닫기·추천/개별/내발주 3탭), roConfirmModal(R1 확정), roCloseConfirmModal(R1 3지선다), roCancelModal(R1 발주취소 사유), roDetailModal(R1 발주 상세)`. **매출 조회(3e)=`🏬 매장→매출 조회` 서브탭 패널**(모달 아님; salesHistoryModal 제거됨). **매장 할인 관리(5-1)=`🏬 매장→매장 할인 상품 관리` 서브탭 패널**(모달 아님, `renderStoreDiscountPanel`; 07-06 설정 카드에서 이전).
> 모든 `.srm-modal`은 `makeDraggableResizable()` 적용. 수정모드 모달은 `safeCloseModal(modal, isEditing, closeFn)`로 종료 확인. `korConfirm()` = `<dialog>` 한글 확인.

## 5.6 카페24 주문 업로드 (매출 공식은 1.4)
- 파일 A~AA 27컬럼. 매칭 키=E열 자체상품코드. 사이즈=H열 `parseCafe24Size`(SIZE=90(L)→L, Size=M→M, 빈값→F). 채널: I열 LEMANGOKOREA→공홈, LEMANGO PARTNER→파트너(`cafe24Channel`). `_cafe24Orders` 주문번호 그룹핑.
- **주문번호 중복 검출(CRITICAL)**: `_buildExistingOrderIndex()` = 전체 revenueLog orderNo Set. 존재 시 `isDuplicate`(파란 배경+취소선, checkbox disabled, 절대 반영 안 됨).
- 행 상태 우선순위: ①중복(disabled) ②환불+미매칭(disabled) ③환불+매칭(checked, 마이너스) ④정상+매칭(checked, 플러스) ⑤미매칭(신규등록 auto 생성, checked). 확정: 정상 `p.sales[ch]+=qty`(type:'sale'), 환불 `-=qty`(type:'refund' 음수).
- 신규 채널 자동감지(`detectNewChannels`/`promptNewChannels`) → 수수료율 입력 → saveChannels + p.sales 키 0 초기화. 미매칭 품번 자동생성(`_cafe24CreateProduct`/`_sbCreateProduct`).
- 사방넷: 20컬럼 A~T, `js/sabangnet.js`. 사은품 판별 = 매출 제외(1.4).

## 5.7 엑셀 상품 업로드/다운로드
- 신규 47컬럼 양식(1행 헤더) `_downloadProductSample()` + 입력 가이드 시트. `ALL_DOWNLOAD_COLUMNS`(마스터 딕셔너리) vs `DEFAULT_FORMATS[*].columns`(실제 export selector) **분리** — 신규 컬럼은 **양쪽 다** 등록해야 함(False-PASS 교훈). 사이즈규격 컬럼은 `buildSizeSpecColumns()`로만 생성.
- 색상 라운드트립: `resolveColorIdentity()`/`_sameColorIdentity()` — 무수정 재업로드 시 거짓 색상 diff 억제 + colorEn 덮어쓰기 방지(마스터 인식 diff, Option B). 색상코드→마스터 조회→colorKr/colorEn 자동 채움.
- 신규기획 라운드트립 양식 `_downloadPlanFull`/`_downloadPlanSample`/`uploadPlans`(sampleNo 필수, confirmed:true 거부). schedule 단계는 `getPlanPhases()` 동적.
- **재고관리 [⬇ 다운로드](`downloadExcel('stock')`, excel.js) = 바코드 관리용 LONG 포맷**: (상품×사이즈) 1행, 헤더 `품번|사이즈|바코드|상품명|브랜드|판매가|수량`, 바코드 미등록=공란(등록현황 가시화), 시트/파일명 `바코드관리`. ⚠️ **앞 3열(품번/사이즈/바코드)은 바코드 일괄 업로드 파서(`sheet_to_json {header:1}` → r[0]/r[1]/r[2] index 기반, stock.js)와 호환** → 다운로드→바코드 채움→재업로드 라운드트립. 상품명/브랜드/판매가/수량은 업로더가 무시(참고 열). 공란 바코드 행은 재업로드 시 '불완전행'으로 skip(채운 행만 반영). 구 WIDE(`품번|…|XS|…|F|합계`, 시트 재고조회)에서 전환(2026-07-03).

## 5.8 CSS
- 브랜드 컬러: `--primary:#1a1a2e`(네이비) `--accent:#c9a96e`(골드) `--bg:#f5f4f1` `--success:#4caf7d` `--warning:#f0a500` `--danger:#e05252`.
- 스크롤: flex 내부 스크롤=컨테이너 `overflow-y:auto`+`min-height:0`, 자식 `flex-shrink:0`.
- 상세 모달 수정모드: `.detail-modal.edit-mode` 클래스로 `.dfield-value`↔input 토글. `.srm-modal{position:fixed;margin:0}`(draggable 필수).
- 2단 헤더 sticky: `fixStickySubRow(tableId)`가 렌더 후 1행 실제 높이 측정→2행 top 동적 적용(하드코딩 금지). 헤더 span 너비 ≡ CSS 컬럼 너비.
- 사이즈규격 input: `.size-spec-input` 스피너 제거(`appearance:textfield`). `size-spec-view-wrap`↔`edit-wrap` 토글.

## 5.9 에이전트 (`.claude/agents/`)
`data-engineer`(엑셀→JSON) · `ui-designer`(레이아웃/컬러) · `search-filter` · `table-renderer` · `excel-handler`(SheetJS) · `dashboard` · `layout-designer` · `feature-builder`(다중파일) · `debugger`(증상→파일) · `code-reviewer`(품질 체크리스트) · `qa-tester`(제3자 검증).

## 5.10 공통 시스템
- **테이블**: `initTableFeatures(tableId, tabKey, renderFnName)`(정렬/필터/리사이즈 통합), `applyColFilters`, 컬럼 드래그(`initColumnState`/`bindColumnDragDrop`), 페이지네이션(`renderPagination`/`goPage`/`getPageSize`), 렌더 시 `State.X.sort`로 정렬(단일 권위). ⚠️ **헤더 컨트롤 주의**: `initTableFeatures`는 그룹헤더(colSpan>1) 외 모든 `<th>`의 innerHTML을 textContent 로 재구성 → 헤더 안에 `<input>`/`<button>`/`<select>`(예: 전체선택 체크박스) 있으면 파괴됨. **컨트롤 보유 `<th>`는 `th.querySelector('input,button,select')` 가드로 조기 스킵**(07-07 수정). 신규 헤더 컨트롤 추가 시 이 가드가 자동 보존.
- **작성자/수정자 스탬프**: `stampCreated(obj)`/`stampModified(obj)` → `renderStampInfo(obj)`. `formatUserName(name,position)`="이름 직급"(사원이면 이름만), `formatUserNameHonorific`. `showUserProfile(uid,anchorEl)` 프로필 팝업.
- **알림**: `addNotification(type,title,body,link,opts)` (link=`#tab:id`/`#tab:personal:id`), `isNotifEnabled(type)` 게이트, 설정 토글(`lemango_notif_settings_v1`). 자동생성: event_start/end, plan_deadline, board_notice, member_pending_urgent(🔴 grade≥2), comment_mention, watch_change, personal_schedule.
- **댓글**: `buildCommentSection(modalType, targetId)` 5개 모달(상세/기획/행사/업무/회원/게시판). `@멘션` 알림.
- **감시(Watch)**: `_watches`, `toggleWatch`/`notifyWatchers`. **편집잠금**: `acquireEditLock`/`releaseEditLock`(5분 timeout). **고정메모**: `pinnedMemo`. **상품 히스토리**: `addProductHistory`/`getProductHistory`(localStorage 50건).
- **자동 백업**(backup.js): 매일 23:59 + 주간/월간, Storage `backups/{type}/{date}.json`(grade≥3 게이트, 실패 토스트 admin-only). `restoreBackup`/`manualBackup`/`getBackupStatus`.
- **전역 에러**: `window.onerror`/`unhandledrejection` → `logActivity('error',...)`(3초 throttle).
- **실시간 동기화**: `setupRealtimeSync`(main.js) — `_onProductsChanged`가 `.filtered` 재구축 + **활성 탭만 렌더**, 비활성 product/stock/sales는 `needsRerender=true`(dirty flag) → `applyTabState` 전환 시 렌더. `buildBarcodeIndex` 재구축. 5분 폴링은 sync로 대체.
- **캘린더 뷰**: 월/주/일(`switchCalView`, `renderWeekView`/`renderDayView`), `getStartOfWeek`, 공휴일(`getHolidayName`), 미니 달력(`openMonthPicker`).

---

# 6. 압축 Changelog (Compact Changelog)
> 완료 sub-phase 1줄 요약. 상세는 git 이력. 🟢=code-reviewer 통과.

## 2026-03 (기반 구축)
- 03-16: 바코드 필드 추가, sizeSpec 분리(bust/waist/hip), 디자인 속성(chestLine/transparency/lining/capRing/guide), GitHub 연동.
- 03-17: saleStatus 필터, 신규입고/개별출고 분리(stockLog/barcodes), 입고 엑셀 long format, `makeDraggableResizable`/`centerModal`, 설정 탭 + `populateAllSelects`, 판매채널 동적화(_platforms), 공홈 주문 업로드, 신규기획 탭 + 상품 이전 워크플로우(품번 중복방지·lock·scheduleLog).
- 03-18: **JS 모듈 분리**(app.js→js/ 15파일) + 해시 SPA 라우팅 + Firebase Hosting 배포. 실제 상품 데이터 로드. 디자인번호/백스타일 `_designCodes` 단일 소스 통합. `korConfirm()` 한글 다이얼로그. **페이지네이션 전 탭 공통**(PAGE_SIZE).
- 03-23: 행사일정 탭(월 캘린더, 컬러 바, 공휴일 `getHolidayName`) + 대시보드 캘린더(행사/기획 연동) + 신규기획 일정 단계·날짜 필터.
- 03-24: 대시보드 레이아웃 2컬럼(캘린더 + 매출/BEST TOP10).
- 03-26: **매출현황 피벗테이블 개편**(2단 헤더, 플랫폼 드래그앤드롭, activePlatforms/inactivePlatforms). 테이블 컬럼 너비 고정. **탭 바 시스템**(멀티탭, DOM 유지 전환, `openTab`/`closeTab`/`resetTabs`). **업무일정 탭**(work.js, 캘린더, 카테고리 색상). **전체 테이블 컬럼 정렬+필터**(`initTableFeatures`, 엑셀 스타일 드롭다운, 교차 필터).
- 03-27: 버그 수정(레거시 app.js 참조 제거, 페이지네이션 컨테이너 복원). sticky 헤더 틀고정. 상품조회 lastInDate 컬럼. 레이아웃 전체 폭 확장.
- 03-30: 2단 헤더 sticky top 버그 수정(`fixStickySubRow`). 샘플 데이터 주입(이후 제거됨). **전체 테이블 컬럼 드래그 관리**(activeColumns/inactiveColumns, 상품/재고/기획).
- 03-31~04-02: dashDayModal(overflow 날짜 상세, 스택 모달, 단계별 서브그룹, 조회 버튼 이벤트 위임). **판매조회→매출현황 이름 변경**. **판매 업로드 모달**(카페24/사방넷/면세점 3탭). **카페24 파싱 전면 개편**(27컬럼, 매출=P+Q−U(MAX)−Y, 주문번호 중복검출, 환불 마이너스, revenueLog type).

## 2026-04 (인증·협업·POS 준비)
- 04-03: **카페24 매출 계산 재구축**(U=주문당 MAX, `_cafe24Orders`, 순실결제). **사방넷 업로드**(sabangnet.js, 매출=H+I, 사은품 제외, 신규등록 자동생성). **로그인/회원관리 시스템**(auth.js/members.js, Firebase Auth+Firestore users, 등급 4단계, 승인 워크플로우, SESSION persistence).
- 04-06: **활동 로그**(activity-log.js, `logActivity`, 회원관리 이너탭). **모달 댓글**(comments.js, 5개 모달). 이모티콘 선택기. **게시판**(board.js, 공지/자유, 첨부 base64). firestore.rules 신규(users/comments/activityLogs/posts). 대시보드 KPI 제거→공지/최근등록. 상품 삭제·생산상태 컬럼. **엑셀 47컬럼 양식**. 바코드 엑셀 업로드. sizeSpec 7사이즈 그리드 + 성별. `safeCloseModal` 수정모드 종료 확인. `korConfirm`→`<dialog>`. 알림 시스템. 행사 모달 보기/수정 분리.
- 04-07: 작성자/수정자 스탬프. **직급 시스템**(POSITIONS, formatUserName). 작성자→프로필 팝업. 신규기획 일괄 일정. **개인일정 시스템**(personalSchedules, @멘션, 가시성 규칙, 관리자 패널). 기획 필드=상품 필드 동일화 + 복제. 로고 이미지 + 로딩 스크린.
- 04-08: 매출 업로드 새 채널 자동감지. 기획 이미지 tempImages(참고)/images(상품) 분리. **9종 기능**(hover/멀티선택/인라인편집/재고경고/채널칩/기획 드래그/단축키/대시보드 필터/캘린더 주·일 뷰+체크리스트). **10종 팀 협업**(상품 히스토리/Watch/팀 활동/@멘션 알림/담당자/칸반/주간 리포트/마감 컬러/편집잠금/고정메모). 신규 가입 최우선 알림(🔴 grade≥2).
- 04-09: 업무일정 차량 사용여부. 대시보드 활동 뷰 등급 기반. 알림 설정 토글. Notification 링크 직접 네비(`#tab:id`). dashInfoModal UI 표준. planScheduleModal 컴팩트. **조직도 탭**(orgchart.js, 부서별 카드, 전화번호). 헤더 3존 레이아웃.
- 04-13: **샘플 데이터 전체 제거** + 일회성 리셋. **자동 백업 시스템**(backup.js). 에러 로그 자동 기록. **Cafe24 CSV 798개 상품 일괄 변환**(products_lemango 633 + noir 165, 품번 prefix→타입 자동감지, 색상코드 매핑).
- 04-14: Firebase Storage 마이그레이션(첨부/기획 임시이미지 base64→Storage, 고아 파일 방지). 신규기획 영속화 버그 수정(savePlanItems). 회원가입 원자성 + 중복 이메일 안내. 회원 승인 즉시 반영(캐시 우회). **checkApproval 보안 구멍 차단**(첫 사용자만 grade4). 5분 자동 새로고침. 업무일정 수정/삭제 권한(canEditWork).
- 04-21: **권한 기반 탭 접근 제어**(TAB_PERMISSIONS + canAccessTab, nav 숨김 + 라우터/hashchange/popstate 가드).
- 04-24: S-01 수정양식 카페24/바코드 제거. S-02 사이즈규격 size-first 그리드. S-03 가이드 섹션 분리. S-04 엑셀 19컬럼 + 사이즈/가이드 HTML 복사.

## 2026-05
- 05-08: S-05 신규기획 엑셀 라운드트립(~70컬럼, sampleNo 필수, confirmed 거부). S-06 신규기획 삭제(작성자/grade≥3, type-to-confirm, Storage 정리).
- 05-XX: **S-07 색상 마스터 시스템**(color-master.js, 111색 10카테고리, 드롭다운 피커, 설정 카드, 마이그레이션, 색상코드 엑셀 통합). 색상 피커 UX 5종. 색상코드 다운로드 누락 수정(False-PASS #1: ALL_DOWNLOAD vs DEFAULT_FORMATS 분리). **S-08 상품 soft-delete + 휴지통 탭**(trash.js, deleted 플래그, 4뷰 필터, 복원/영구삭제). S-09 휴지통 조회 모드(readOnly) + dLockCodeBtn 버그 수정(False-PASS #2: helper 호출 후 override).
- 05-21: **Phase A 보안 수정**(firestore.rules): 회원 self-update grade/status 불변, 삭제 규칙 3건 동기화(comments/leaves/users), personalSchedules 작성자 전용, isApproved 게이트. **Phase A-2 U1**: users create grade1/pending 강제. 신규기획 품번 생성 시리얼 슬라이스 버그(prefix 11자) + 수정 시 품번 필수 차단 제거. 백업 Storage 403 핫픽스(cross-service grade→auth!=null + JS 게이트). 백업 Firestore→Storage 이전(1MB 한도 회피, Option C). 품번 분류 dropdown `_classCodes` 라이브. 진단 문서(permission-model/pre-activation-audit).

## 2026-06
- 06-22: **사이즈 규격 Phase A**(SIZE_SPEC_PARTS 단일 소스 리팩터, buildSizeSpecColumns 19컬럼, 동작 동일) → **Phase B**(측정부위 4종 추가, core.js 1파일, 43컬럼) → **Phase C**(빈 측정항목 제외 getActiveParts, 보기/HTML filled-only). `fc04ef1`/`d602aa8`.
- 06-23: **색상 라운드트립 버그 수정**(`8e66142`, resolveColorIdentity/_sameColorIdentity, 마스터 인식 diff, Option B). **재고/매출 상품 누락 RC1**(`f6d6cf6`, refreshAllProductViews 헬퍼, sales.filtered 누락 4경로). **휴지통 stale RC2 2a**(`e166bab`). **기본 정렬 registDate desc + 재고/매출 브랜드 필터**(`1198c8c`). **RC2 2b**(dirty flag 타 탭 stale + 검색 영속화 searchCriteria/_reNarrowFiltered) — 재고/매출 stale 시리즈 완결.
- 06-24: 사이즈 규격 비키니 상/하의 4종(11부위 67컬럼) + 컵/허리 4종(15부위 91컬럼) — 각 core.js 1파일. 사이즈규격 input 숫자 스피너 제거(CSS).

## 2026-06-30 ~ 07-02 (POS)
- 07-08: **Phase 2 개정 — 상품등록 양식 컬럼정리 + 코드목록 시트 가독화 + 색상코드 참조**(🟢) — Phase 2 양식 생성기(`downloadProductTemplate`) 개정. **R1** 이미지URL 3열 제거(대표/자사몰/외부몰) → **127컬럼**. **R2 백스타일=드롭다운→자유입력**(99개 드롭다운 과다 → 코드목록 시트 조회 후 코드 직접 입력, note "코드목록 시트 참고"; RANGE/드롭다운맵에서 design 제거·`_designRef` 참고전용). **R3** 드롭다운 컬럼 헤더=그린(팔레트 `--success`, 필수=골드·그 외=네이비와 구분). **A1 색상코드=코드목록 참조**(백스타일 패턴): `_colorMasters`(active, LIVE) 111색을 코드목록 E/F/G 섹션(코드/한글/영문); 색상코드 열=자유입력+note(Phase 3 색상명 자동). **A2 코드목록 시트=VISIBLE·사람이 읽는 참고**(veryHidden 제거): 안내 타이틀 + 백스타일 섹션(A/B/C) + 색상 섹션(E/F/G) + 드롭다운 목록(I열~, 스페이서 D/H) · 스타일 헤더/너비 · Ctrl+F 검색. 🔴 **range-ref 정합**(리뷰 핵심): 관리리스트 dropdown = `코드목록!$<letter>$5:$<len+4>`가 실제 쓰기 컬럼(getColumn(rcol).letter)·행과 byte-identical(레이아웃 이동 시 rangeRef 동기) — merge 겹침 0(리뷰 확인). 파서 계약(후행 (code)) 불변. **⚠️ 미수령 base "Phase 2 Revision" 지시서** — 개정 3종은 addendum 요약대로 구현(이미지 제거=3열 전부·드롭다운 헤더=그린 등 문서화 결정, 소유주 확인 요청). 업로드 파서=Phase 3 미구현. code-reviewer 🟢(6/6, range-ref/merge PASS). `--only hosting`.
- 07-08: **ExcelJS 추가 + 신규 상품등록 양식 인셀 드롭다운 (Phase 2 상품등록 개편)**(🟢) — **ExcelJS CDN 추가**(index.html, `exceljs@4.4.0`, SheetJS 형제 — XLSX/ExcelJS 별개 전역 공존). 🔴 **ExcelJS=양식 생성 전용 · SheetJS=모든 읽기/파싱/round-trip 유지**. 신규 `downloadProductTemplate()`(excel.js): 소유주 확정 순서 **130컬럼**(비사이즈 36[+CAFE24/사방넷 상세 URL **신규 2열**=제조국 뒤] + 사이즈규격 91[`buildSizeSpecColumns` 단일 소스] + 판매상태/생산상태/등록일 3; 소유주 "134"는 사이즈 블록 근사) + **숨은 코드목록 시트**(범위참조 소스). **인셀 드롭다운**(`ws.dataValidations.add('col2:col301'…)`, 300행=업로드 상한): 고정세트(브랜드/분류/성별/타입/연도/시즌)=**인라인**("이름(코드)") · 관리+긴 리스트(디자인 99·레그컷·원단타입·세탁방법·가슴선·비침·안감·캡고리·판매상태·생산상태)=**범위참조**(코드목록 시트, **LIVE**=Phase 1 설정 편집 반영). `downloadSample('product')`(상품조회 탭 + 등록 모달 버튼)→신규 양식 라우팅, **ExcelJS 미로딩 시 toast+구 SheetJS 양식 폴백**(무크래시, 업로드 무관). 🔴 **Phase 2↔3 파싱 계약**: 라벨 후행 `(code)` — `extractCode(v)=/\(([^)]+)\)\s*$/ 매칭 시 그 안, 없으면 값 전체`(디자인 `${en} ${kr}(${code})`도 코드 괄호 항상 마지막 → 앵커 정규식 안전; 시즌/단순 관리리스트=값 자체=코드). `showErrorMessage:false`(소프트 드롭다운 — **실제 강제는 Phase 3 업로드 검증**). 자유필드(판매가/원가/샘플번호/상품명/URL/사이즈규격)=드롭다운 없음·품번=드롭다운 없음+note. **업로드 검증/자동채움/품번차단=Phase 3**(미구현). 기존 SheetJS export(stock/sales/plan round-trip)·매출공식 무손. code-reviewer 🟢(7/7 PASS). `--only hosting`. ⚠️ **신규 양식의 매칭 업로드 파서=Phase 3** — Phase 3 배포 전엔 신규 양식 업로드 지양(구 파서와 컬럼 불일치).
- 07-08: **세탁방법·생산상태 관리형 리스트 추가 (Phase 1 상품등록 템플릿 개편)**(🟢) — 기존 `SETTING_DEFS`(core.js) 관리형-옵션 패턴에 `washMethods`(세탁방법·group design)·`productionStatuses`(생산상태·group info) 2개 추가 → 설정 탭이 `_renderSetCard`(제네릭, def.key 키잉)로 **CRUD 카드 자동 생성**(신규 함수 0). `DEFAULT_SETTINGS`에 기본값(`washMethods:['손세탁','드라이클리닝','기계세탁']` · 🔴 `productionStatuses:['지속생산','생산중단','시즌한정','샘플']`=**기존 rProductionStatus 하드코딩 세트와 byte-identical → 기존 상품 값 무효화 없음**). `populateAllSelects` rProductionStatus를 하드코딩 배열→`s.productionStatuses`로 전환(레그컷/원단타입은 이미 관리형·무변경). 🔴 **세탁방법=폼 select 없음**(register/plan/detail 전부 textarea=상세 케어 자유텍스트, 의도적) → textarea 유지, 리스트는 관리 카드+Phase 2 엑셀 드롭다운 소비(강제 변환 안 함). 🔴 **backfill(리뷰 발견 블로커 수정)**: `_settings` 로드 3경로 중 **2개 delete-all+교체 경로**(`_fsReloadSharedSettings` core.js:284 · realtime `case 'settings'` core.js:561)가 신규 키 없는 구 Firestore 문서로 전면 교체 → 신규 기본값 소멸 → `populateSelect(undefined)` 크래시. **누락 키만 backfill**(기존 값 불변) 양 경로 추가 + `populateSelect` `Array.isArray` 가드(이중 방어). 기존 상품 데이터 무접촉(옵션만 제공)·매출공식 무손. code-reviewer 🟢(재리뷰 — 블로커 해소). `--only hosting`.
- 07-08: **위치 이동 창 — 로케이션 엑셀 다운로드 + 업로드 (LOCATION ONLY)**(🟢) — 위치 이동 창에 `[⬇ 로케이션 다운로드]`/`[⬆ 로케이션 업로드]`. 🔴 **로케이션만 반영 · 재고 수량·바코드 무변경**(업로드 확정도 위치 이동 apply 재사용 = sizeLocations-only write + location-move 원장 · grep 증명: sizes/defectSizes/barcodes/increment 0건). **다운로드**(`downloadLocations`, store-scoped): 현재 매장 stock 품번 × 전 사이즈 → `품번|사이즈|바코드|로케이션(현재)|상품명|브랜드`(뒤 2열 참고용, 바코드=식별 전용 round-trip) · 파일명 `로케이션_{매장}_{KST}.xlsx`. **업로드**: `handleLocUpload`(sheet_to_json raw:false=앞자리0 보존) → `_locUpResolveRow`(**1e `_ssuResolveRow` 듀얼-shape 미러**: 바코드 우선 findByBarcode→code/size·품번/사이즈 병기 교차검증→불일치 · 아니면 품번+사이즈 · **바코드 식별 전용 절대 미기록**) → 로케이션 normalizeLocation · 빈값=skip · 현재와 동일=no-op skip · 시트내 (품번,사이즈) 중복=첫행만 · **미리보기 표**(반영/변화없음/미등록/불일치·중복/빈위치 배지+카운트, 1e UX 미러) → `[확정]`(반영≥1). **확정=safe-chunk**(위치=SET-overwrite·상쇄 아님 → 청크 안전, 6d baseline 근거): 200행/배치(코드 storeStock merge-set + 라인 location-move 원장 doc, 결정적 id `{locNo}_{글로벌seq}`, <500 여유) · 중간 실패=반영 청크 무손상+부분 보고("재업로드 시 no-op-skip 안전 재개") · `_locUpInFlight` 가드. 원장 memo='엑셀 업로드', **non-cancellable 상속**(moveType 동일). 6d 대조 무영향(stockDelta:0). 본인매장+admin(storeStock/storeInbound write 규칙 서버 강제) · 매장 컬럼 없음(활성 매장). **규칙/인덱스 무변경**(field-agnostic·2d/6a 인덱스 재사용) → `--only hosting`. 매출공식·재고·바코드 업로드·1e·상호작용 위치이동 무접촉. code-reviewer 🟢(9/9 PASS).
- 07-08: **위치 이동 (Location-Move) — 재고 무변경 + 이동 원장 기록**(🟢) — (품번,사이즈)의 로케이션만 옮기고 **재고 수량은 절대 변경 안 함**. 재고현황 허브 `[📍 위치 이동]`(본인매장+관리자, resolveActiveStore null=비활성) → 6a 재고수정 창 패턴 미러(명시적 닫기·3지선다·품번 조회 전 사이즈·스테이징·draft `lemango_locmove_draft_{store}`). 🔴 **재고 무변경=구조적**: 확정 storeStock write payload = `{storeId,productCode,sizeLocations,updatedAt}` **only**(sizes/defectSizes 키 없음·increment 없음·writeStoreStock 없음 → grep 증명) → 수량 변경 물리적 불가. **이동 기록**=storeInbound `moveType:'location-move'`·`stockDelta:0`/`defectDelta:0`·`fromLocation`/`toLocation`·결정적 id `{LOC-…}_{seq}` → **exactly-once**(재시도 set=update→storeInbound 취소규칙 거부, 6a 미러 · `_pendingLocNo` draft 영속 · 프리플라이트 `_adjReadDoc` 재사용 · `_locInFlight` 가드 · denied 재확인). **normalizeLocation 적용**(trim-only 고아 `setStoreStockLocation` 재사용 금지). **no-op 제외**(새위치 빈값 or 정규화 동일 → `_locActionable` filter) · **최초지정 허용**((없음)→new, Q2) · **재고0 경고**(korConfirm, 진행 허용·차단 아님, Q2). **단일 원자 배치**(청크 금지, codes+lines>450 차단, Q3 멀티라인). 🔴 **취소 불가(Q4)**: 입출고 내역(2d) location-move 행=취소 버튼 대신 '이동' 힌트(`canCancel=false`) + `requestInbCancel` 방어 가드(일반 취소는 옛 위치 미복원=반쪽 취소 → 되돌리기=반대로 재이동). **6d 대조 zero-impact**(stockDelta:0 → 누계/이론치 0 기여 — 검증). 표시: `_mvTypeLabel`/`_ldgTypeLabel` '위치이동' 라벨 · `_mvNote` from→to(+메모) · 입출고 내역 유형필터 `__location-move__` · 정상증감 '-'. **규칙/인덱스 변경 없음**(storeStock write·storeInbound create 규칙 field-agnostic · 2d/6a 인덱스 재사용) → `--only hosting`. 매출공식·6a~6c·판매·할인·재고현황 검색 무접촉. code-reviewer 🟢(10/10 PASS).
- 07-08: **로케이션 서브탭 제거 → 재고현황 검색(바코드 정확/품번 부분) + 불량 재고 목록**(🟢) — placeholder 였던 `location` 서브탭을 STORE_SUBS 에서 제거(orphan 0, grep clean — 유일 참조는 STORE_SUBS 항목 1줄, dispatch 없음·generic placeholder fallthrough 로만 표시됐음). 대체 기능을 **재고현황(1f) 뷰 상단 인라인 검색 바**로 신설. **① 검색(자동감지)**: 입력 trim → **(1) 등록 바코드 정확일치 우선**(`findByBarcode` 역인덱스[core.js:853, O(1), soft-deleted 제외]) → (품번,사이즈) 해소 → **공유 상세 모달**(`openStoreProductDetail`, 사이즈별 정상/불량/로케이션 6c) 오픈 + 매칭 사이즈 표시 · **(2) 아니면 품번 부분일치**(대소문자 무시 substring, **현재 매장 재고 범위**=`_storeStockIndex[store]` 키) → 결과 목록(품번/상품명/총재고) → 클릭 시 공유 모달. 🔴 **우선순위 = exact-barcode-first**(바코드로 등록된 값이면 품번 부분일치보다 우선). 검색은 `#ssvAuxPanel` 에만 렌더(상품 테이블 재빌드 없음). **② 불량 재고 목록**: `[🔴 불량 재고]` 버튼 → `buildStoreStockIndex` 재빌드(6c 전환 즉시 반영) → `_storeDefectIndex[store]` 순회 → **defectSizes>0 (품번,사이즈)만** 행(품번/사이즈/불량수량, 소유주 최소 요구) · 품번 정렬→사이즈 SIZES 순 · 행 클릭=공유 모달 · 빈 목록="불량 재고 없음" · 엑셀(품번/사이즈/불량수량). store-scoped(현재 매장). 🔴 **로케이션 UI 재구축 없음**(공유 모달이 이미 사이즈별 정상/불량/로케이션 표시 — 검색이 그리로 라우팅만). **재고 write 0건**(순수 조회). 매출공식·6a~6d·판매/입고·할인 무접촉. CSS 매장 전용 클래스(`.ssv-search-*`/`.ssv-aux-*`) 신설(전역 무변경). code-reviewer 🟢. `--only hosting`.
- 07-07: **매장 테이블 엑셀식 컬럼 드래그 리사이즈 (B2)**(🟢) — 매장 서브탭 테이블(재고현황/매출조회/물류/입출고/품목원장) 컬럼 경계 드래그로 개별 폭 조절. 🔴 **`initTableFeatures`(utils.js:730) 매장 테이블에 호출 금지**(헤더 재구성+sort/filter 가 State[tabKey]/data-key 전제 → 커스텀 헤더 파괴) → 리사이즈 로직(utils.js:798)만 추출한 **독립 헬퍼 `makeStoreColumnsResizable(tableEl, key)`**(비파괴 — 핸들 div 만 append). B1 auto-fit 기본, **첫 드래그 시 현재 폭 seed(read-all-first) 후 `table-layout:fixed` 전환**(다른 컬럼 점프 방지), 이후 저장 폭 재적용. **영속=localStorage `lemango_store_colw_{key}`**(컬럼 index 기준) — 🔴 **메인 탭 `State[tabKey].colWidths` 와 완전 별개 네임스페이스**(무충돌). ⚠️ **`th.position` 절대 미변경**(매장 th=`position:sticky`=positioned → `.col-resize-handle`(absolute) containing block; initTableFeatures 의 `position:relative` 는 의도적 제외 → sticky 보존). idempotent(핸들 중복 가드) + document mousemove/mouseup onUp 해제(누수 없음) + `sum>0` 저장 가드. 5개 렌더에 wiring(ssv/shist/lg/inbhist/ledger 고유 키, 공유 `.inbhist-table` 는 `tbodyId.closest('table')` 로 정확 선택). **메인 탭(product/stock/sales/plan) resize·colWidths 무변경**(additive, initTableFeatures 무손). 기존 `.col-resize-handle` CSS 재사용(CSS 무변경). code-reviewer 🟢(6항목 PASS). `--only hosting`.
- 07-07: **매장 테이블 auto-fit (B1) — 클리핑 제거 + 가로 스크롤 (store-scoped CSS-only)**(🟢) — 매장 서브탭 테이블(재고현황/매출조회/물류/입출고내역/품목원장 등)이 `table-layout:fixed`(전역 `.data-table`) 상속으로 상품명/품번 컬럼이 잘리던 문제. 🔴 **전역 `.data-table`(style.css:539) 미변경** — 매장 전용 클래스만(`.ssv-table`=재고현황, `.inbhist-table`=매출조회/물류/입출고/품목원장/발주 공용 베이스). 픽스: 매장 테이블에 `table-layout:auto` + `width:auto` + `min-width:100%`(좁으면 패널 채움·넓으면 내용 이상으로 grow→래퍼 가로 스크롤) + 헤더 `overflow:visible/text-overflow:clip`(전역 ellipsis 해제) + `.ledger-table-wrap` `overflow-x:auto` 보강. auto 레이아웃이라 컬럼이 내용에 맞춰짐 → 전역 td ellipsis 는 오버플로 미발생으로 inert(무해). **메인 탭(상품조회/재고/매출/신규기획/휴지통) 무영향**(fix 클래스 미보유·grep 증명 · `.data-table` fixed+영속 colWidths 그대로). CSS-only(JS 무변경). **B2(엑셀식 컬럼 드래그 리사이즈)=별도 후속**. code-reviewer 🟢(store-scoped/no-clip/specificity PASS). `--only hosting`.
- 07-07: **보충 발주 Group A 수정 3종 (개별추가 실사이즈 · 추천 품절만 · 전체 담기)**(🟢) — ① **개별추가 바코드 사이즈 제한**(버그픽스): `selectRoManualProduct` 사이즈 피커가 전 7 SIZES → **`_inbBarcodedSizes(p)`**(판매/입고와 동일 실사이즈 소스=바코드 등록 사이즈)만 표시 · `renderRoManualResults` 검색도 바코드 사이즈 0인 상품 제외(판매 lookup 미러) · 바코드 0 상품=친절 안내. ② **추천 품절만(오너 결정 ⓑ)**: `_roComputeSuggestions` inclusion = `순판매>0 AND 재고≤0`(재고 있으면 제외, 음수 oversold 포함) — `getStoreStock`(sizes only, 불량 격리) · 제안수량 기본=순판매(편집 가능) · 재고/요청중 컬럼 유지 · info "품절(재고≤0)만 표시" 노트. ⚠️ **개별추가는 재고 무관 유지**(바코드 사이즈면 재고 상관없이 주문 가능). ③ **[➕ 전체 담기]**: `roAddAllSuggest` — 표시된 추천 전부를 현재(편집된) 제안수량으로 staging 추가(`_roStageAdd` merge, `silent` 4th param 로 per-item 토스트 억제 → 요약 1건) · 빈 목록 가드 · staging 편집 유지. 🔴 **DB write 0줄 추가·submit(buildReplenishDoc/roConfirmProceed/exactly-once) 무변경·재고 무접촉**(순수 UI/필터). code-reviewer 🟢(4속성 PASS·manual-path stock leak 없음). `--only hosting`.
- 07-07: **물류 발주 확인 뷰 → 매장 서브탭으로 이전 (relocation-only)**(🟢) — R2 물류 뷰를 top-level `🚚 물류` 탭에서 **🏬 매장 탭의 '물류 발주 확인' 서브탭**으로 이전(오너: 매장/물류 직원만 사용 → top-level 불요). **순수 relocation, 동작 변경 0**: `STORE_SUBS` 에 `logistics` 서브탭 추가 + `_storeSubPanelHtml` logistics 분기(`#logisticsBody` 온디맨드, discount 패턴 미러) + `renderStoreTab`/`switchStoreTab` dispatch. `renderLogisticsTab` 은 컨테이너 타깃만 `#logisticsPage`→`#logisticsBody`(+ top-level store-header 제거) 2줄 변경 — `_lgLoad`/`_lgApplyFilters`/`lgCheck`/`openLgDetail`/`downloadLgOrders` **byte-unchanged**. **top-level 클린 제거**(orphan 0): nav 버튼·`tab-logistics` 섹션·`TAB_PERMISSIONS.logistics`·router(case/applyTabState/no-products 예외) 전부 삭제(grep clean). 접근=매장 탭 권한(grade≥1) 관장, 서브탭 추가 게이트 없음(전 승인직원=R2 동일). field-restricted 체크·규칙·인덱스·A2 lifecycle·zero-stock-write 전부 무변경. code-reviewer 🟢(5항목 PASS). `--only hosting`.
- 07-07: **POS 보충 발주 R2 — 물류 확인 뷰 (확인✓/발송✓) · ✅ 보충발주 R1+R2 완료**(🟢) — 신규 **🚚 물류 탭**(전 승인직원 v1, `TAB_PERMISSIONS.logistics:1` + router dispatch + applyTabState 매-오픈 재조회). 🔴 **READ 뷰 + 표시전용 진행 체크, 재고 무접촉**(storeStock/storeInbound/storeSales write 0건 grep 검증 — 유일 write=`replenishOrders.update`). **조회**: `where(dateKey>=,<=)` 단일필드 range(전 매장, auto 인덱스 — (status,dateKey) 복합 미사용→예약 유지) + 클라 매장/상태 필터 + createdAt DESC. 컬럼 RO번호/일시/매장/건수·수량/상태/확인✓/발송✓/메모, 행→상세(R1 `roDetailModal` 재사용, 품번→공용 상품상세). **확인✓/발송✓**(`lgCheck`): R1 예약 필드를 **field-restricted update**(정확히 3필드 `{confirmChecked:true,confirmCheckedBy:uid,confirmCheckedAt}` 또는 ship 3필드)로 **false→true 단방향**(R1 규칙 (b) 분기: 승인자 전원·hasOnly 3필드·setter==uid·status==requested). **되돌리기 없음**(체크되면 "✓ 이름 시각" 최종; 이름=confirmCheckedBy uid→`_allUsers` 클라 해소, 규칙이 name 필드 불허). 취소 발주=체크 불가('—', 규칙도 차단). **race**: korConfirm dedup + `_lgCheckInFlight` + 서버 `resource.confirmChecked==false` → first-writer-wins·패자 `_lgLoad` 재조회. logActivity('replenish-check'/'replenish-ship'). **엑셀**(발주현황 11컬럼). ⚠️ **A2 lifecycle**(R1 무변경 확인): `shipChecked=true` → 매장 추천 **요청중 제외** + **취소경로 봉쇄**. 규칙/인덱스 변경 0(R1 예약 재사용) → `--only hosting`. 매출공식·재고·R1 판매경로 무손. code-reviewer 🟢(5대 속성 PASS · 이중렌더/플래그타이밍 2건=무해). **✅ 보충 발주(R1 생성 + R2 물류확인) 완료** — 마감/부분발송/최소재고=v2.
- 07-07: **전체선택(select-all) 헤더 체크박스 복구 (initTableFeatures 버그)**(🟢) — 신규기획 전체선택이 동작 안 함(오너 리포트). 🔍 **근본원인**(라이브 브라우저 실측): `initTableFeatures`(utils.js)가 그룹헤더 외 **모든 `<th>`의 innerHTML을 `th.textContent` 기반으로 재구성**(`th.innerHTML = inner`) → 헤더 내 인터랙티브 컨트롤 파괴. 전체선택 th(plan.js:793 `<label><input id="npCheckAll" onchange="togglePlanCheckAll">전체선택</label>`, rowspan=2·data-key 없음)가 `colSpan>1` 가드에 안 걸려 체크박스 소멸(텍스트만 잔존) → 클릭 대상 없음. tbody 행 체크박스는 무관(정상). **a15cc99 회귀 아님 — initTableFeatures는 오래전부터 불변**(일괄확정 추가로 전체선택을 처음 쓰며 잠복버그 노출). **픽스**: `colSpan>1` 가드 다음에 `if (th.querySelector('input, button, select')) return` 1줄 추가 → 컨트롤 보유 헤더 스킵(보존). 데이터 컬럼(라벨 텍스트)은 미영향(정렬/필터/리사이즈 정상). 🔴 **부수효과(리뷰 발견)**: **상품조회 전체선택**(products.js:133 `_check` 헤더=`<input onchange="toggleAllProdCheck">`, id 없어 grep 누락)도 **동일 버그로 잠복 파손** → 이 픽스가 **동시 복구**. stock/sales 헤더=label-only → 가드 미발동(무변경). 공유 함수지만 4개 테이블 전부 안전(실측: 헤더 컨트롤=plan+product 2곳뿐). 브라우저 실측 검증(전체선택/해제/tri-state OK). code-reviewer 🟢. `--only hosting`.
- 07-07: **신규기획 상품 일괄 확정 (bulk product confirm)**(🟢) — 신규기획 목록(기존 checkbox 선택 + pending 기본필터 재사용)에서 미확정 기획을 **한 번에 상품 확정**. `applyBulkSchedule` 패턴 + npToolbar `[✅ 상품 일괄 확정]` 버튼. **① 검증 패스**: 선택 pending 각각 (품번 없음 / 이미 존재 / **배치 내 중복**(running Set)) → toConfirm/skipped 분할, 이미확정 선택분은 별도 스킵. **② 단일 확인 다이얼로그**(korConfirm, count + skip preview 8건, 기본포커스=취소; 확정 0건이면 사유별 집계 토스트) — N개 개별 confirm 아님. 🔴 **③ 원자 저장(dual-write 위험 제거)**: `_buildProductFromPlan`(단일/일괄 공용 빌더, build→push 순차로 no 연속) + `_stampConfirmedBy`(uid/name/at, 상품+기획항목 양쪽) → in-memory 반영 → **단일 `db.batch()`**(products 청크 전체 + products_meta + planItems, 전부 sharedData 문서 → 한 커밋 원자; saveProducts 청킹 미러 `_FS_PRODUCT_CHUNK`=150) → **apply+build+commit 전체를 하나의 try 로 감싸 어느 예외든 in-memory 완전 롤백**(origLen truncate + 기획항목 변경필드 복원, 없던 confirmedBy/At delete) → **half-applied 구조적 불가**. 성공에만 localStorage/`_lastProductSaveTime`/`_lastSharedSaveTime` 갱신(에코 억제). 후처리 1회: `refreshAllProductViews` + `searchPlan`(확정분 pending 필터 이탈) + logActivity + addProductHistory. **navigation 없음**(탭전환/N모달 X, 요약 토스트). **권한 게이트 없음**(단일 확정과 동일·오너 결정) BUT **확정자 기록**(추적성). **단일 확정=behavior 무변경**(공용 빌더 사용 + confirmedBy 스탬프만 가산; 네비게이션/persist 유지). ⚠️ 단일 확정의 fire-and-forget 이중 write 는 **의도적 유지**(오너: 단일 behavior 불변) → 일괄 경로만 원자(§4 잔여). POS/매출공식/재고 무접촉(sellable-product 데이터만). code-reviewer 🟢(4대 속성 PASS · 리뷰 후 롤백을 전체 try 로 확장 + clonePlanItem confirmedBy 리셋 반영). `--only hosting`(규칙/인덱스 무변경).
- 07-07: **POS 보충 발주 R1 — 매장 발주 생성(추천 + 개별추가)**(🟢) — 신규 `replenishOrders` 컬렉션(요청 문서). 🔴 **재고 무접촉**(storeStock/storeInbound/storeSales write 0건, grep 검증 — storeSales 는 추천 계산용 읽기 전용 스캔만). 보충대상조회 허브 버튼 → 명시적-닫기 발주 창(ESC/백드롭 차단, staging 3지선다, 작업게이트=본인매장+관리자·office 게이트). **① 추천**(`_roComputeSuggestions`): 마지막 **미취소** 발주 createdAt 이후(없으면 7일) 순판매(sales−voids) per (품번,사이즈) — storeSales 기간스캔[3a 인덱스]+클라 라인필터+ts≥anchor 정밀 · 컬럼 순판매/현재재고/**요청중(=status'requested'&&!shipChecked, A2 무한누적 방지)**/제안수량(편집) → 담기. **② 개별추가**: 품번 조회(바코드 무관 **전 상품·전 사이즈**) → 담기. 공용 staging(동일 code+size 합산) + 메모 + draft(`lemango_replenish_draft_{store}`, pendingRoNo 동결). 🔴 **exactly-once 확정**: doc id=roNo(`RO-`+kstStamp+8자접미, 단일 now) 결정적 · 단일 `.set()`(create 의도) → 재시도 set=update 평가 → replenishOrders update 규칙(취소/체크 한정) 거부 → 중복발주 불가 · 프리플라이트+15s 타임아웃+denied 서버존재조회 판별(3c 미러) · `_roInFlight` 동기 가드. **내 발주**: 기간조회(storeId,dateKey 신규 인덱스)+상세 모달+엑셀+**취소**(status'requested'&&미발송 · 사유 필수 · field-restricted update[status/cancelledBy/At/Reason 4필드] · 1회성). **규칙**: replenishOrders read=승인자 · create=본인매장/관리자+shape(requested·체크 false) · update=(a)취소[요청·미발송 게이트, 4필드 hasOnly, cancelledBy==uid, 사유필수] (b)R2 확인✓/발송✓[요청상태·false→true 단방향·각 3필드 hasOnly·setter==uid] · delete=false · Map.get 방어. **R2(물류 뷰·발송 처리)=DEFERRED**(체크 필드/규칙/인덱스 예약 → R2 hosting-only). 규칙+인덱스 배포 필요 → `--only firestore:rules,firestore:indexes,hosting`. 매출공식·재고차감·3c/3e/6a~6d·5-x 무손. 설계문서 `pos-replenish-design.md` 부재(작업지시+오너조정 self-contained → 지시서 authoritative 로 진행). code-reviewer 🟢(4대 속성 PASS · 리뷰 후 roNo 단일-now·체크분기 status 가드 2건 반영).
- 06-30: **POS Phase 0**(바코드 업로드 하드닝: 13자리 검증/유니크/raw:false + 역인덱스 buildBarcodeIndex/findByBarcode). 캐시 무효화(firebase.json Cache-Control) + 바코드 저장 하드닝(saveProducts 반환값). 바코드 대소문자 무시 + 미등록 복사 + 불완전 행 경고.
- 07-01: POS Phase 1 설계 문서. **Phase 1a**(매장 config `_stores`) → **1b**(사용자 storeId + resolveActiveStore) → **1c**(매장 탭 shell store.js + 서브내비 + 관리자 스위처) → **1d**(storeStock per-doc 모델 + FieldValue.increment 헬퍼 + Firestore 규칙, SIZES 확정, OR-union 정정) → **1e**(재고 엑셀 SET/ADD, 이중 식별) → **1f**(매장별 재고현황 뷰 + 상세 모달). Phase 2 설계 문서. **2a**(sizeLocations 데이터층, 재고 코어 불변). **✅ Phase 1 완료**.
- 07-02: **Phase 2b**(입고 스캔 화면: 커서 상태머신 + 4대 차단규칙 + staging + localStorage draft + IME/디바운스, 재고 쓰기 없음) → **2b-r**(서브메뉴 6→4, 허브 창 재하우징 inboundScanModal, UX 재설계, 품번조회 inbLookupModal, 5종 폴리시, 입고 오류 배너 버그 수정) → **2c**(최종 확정 원자적 배치: storeStock increment + sizeLocations overwrite + storeInbound 이력, double-confirm 가드, 청크 코드 경계, 규칙+인덱스) → **2d**(입고 내역 날짜별 조회). **✅ Phase 2 완료**. 입고 이력 업그레이드(기간조회/입고번호 IN-.../메모/엑셀). **입고 취소**(runTransaction 역반영 increment(-qty) + 사유 필수 클라+서버 + 취소필드 field-restricted update, 코드베이스 첫 트랜잭션). 입고 물류 강화(_inboundTypes 유형 설정 + 확정 시 유형 스냅샷 + 유형/상태 필터 + normalizeLocation).
- 07-02: POS Phase 3 설계 문서. **Phase 3a**(매출 원장 데이터층: buildSaleDoc/buildVoidDoc/generateSaleNo/normalizePhone/maskPhone/voidDocId, storeSales 규칙 refinement, 복합인덱스, customerPhone, double-void 구조적 불가 증명). **Phase 3b**(판매 화면: 스캔→즉시 라인 추가/병합→합계→draft, saleLookupModal, 확정 stub, 재고/원장 쓰기 없음). **공용 상품 상세 모달**(openStoreProductDetail, 판매 리스트 + 1f 재고뷰 공용, 이미지/상품명/사이즈별 재고·로케이션, 읽기전용).
- 07-03: **POS Phase 6a 재고수정(실사) + 이동 원장 앵커**(🟢) — `storeInbound` 를 per-line 이동 원장으로 확장(`moveType:'adjust'` + signed `stockDelta`/`defectDelta`; 레거시 backfill `_mvType`/`_mvSignedQty`/`_mvTypeLabel`/`_mvNote`). 재고현황 허브 [🛠 재고수정](본인매장+관리자) → 명시적-닫기 창(ESC/백드롭 차단, main.js cancel-preventDefault): 품번 조회(전 상품, **바코드 무관 전 7사이즈** — 실사는 모든 재고 대상, 입고와 의도적 차이) → 사이즈 선택 → ±증감 인라인(0 입력=라인 삭제) + 로케이션 편집 → 스테이징. 상쇄 세션(A −1/B +1 net0) 허용. draft `lemango_adjust_draft_{store}`(+pendingAdjNo). **확정=단일 원자 배치**(청크 절대 금지 — 상쇄 부분반영 방지; codes+lines>450 차단): 코드별 storeStock merge-set `sizes:increment(±)` + `sizeLocations`(편집·비공란만) + 라인당 이동 doc `{adjNo}_{NNNN}` **결정적 id**(set=create). 🔴 **exactly-once**: 재시도 시 동일 id set=update 평가 → storeInbound update 규칙(`cancelled==true` + affectedKeys 취소5필드만) 위반 → 배치 전체 거부 → 재반영 불가. pendingAdjNo=draft 영속(3c pendingSaleNo 미러), adjNo=`ADJ-`+kstStamp+`-`+8자 접미사(generateSaleNo 강도). 프리플라이트(onLine + `{adjNo}_0001` 서버read, 존재=착지=성공처리) + 15s 타임아웃 + denied 착지 재확인. 사유 필수(클라 — 규칙은 create-reason 강제 불가, 신뢰모델 C13). Q4 +조정 로케이션없음 = korConfirm 경고+진행. **2d "입출고 내역"으로 일반화**: moveType 유형 라벨(재고수정/입고유형)/부호 증감(음수 빨강)/사유·메모/재고수정 필터(`__adjust__`)/엑셀; 취소=stockDelta 부호반전(`increment(-delta)`, 레거시=−qty, defectDelta 가드). 신규 인덱스 `storeInbound(storeId,productCode,dateKey)`. logActivity('adjust','재고수정'). **배포=`--only firestore:indexes,hosting`**(규칙 무변경). 다음=6b 반출.
- 07-06: **POS Phase 6b 반출(Outbound)** — 입고 스캔 창에 **방향 토글(정상입고/정상반출)** 추가(`inbSetDirection`/`_inbApplyDirectionUI`/`_inbDirection`, 기본 정상입고). 반출=음수 movement: `moveType:'outbound'`·`stockDelta:-qty`·번호 `OUT-`+kstStamp+8자접미사(`generateOutNo`). 스캔·품번조회·staging·entry 카드·리스트 전면 재사용, **확정만 분기**(`inbFinalConfirm` 첫 줄 dispatch→`_outFinalConfirm`/`_outConfirmProceed`): 코드별 storeStock merge-set `sizes:increment(-qty)` + 라인당 이동 doc `{OUT-…}_{NNNN}` **결정적 id**(set=create) + `_pendingOutNo`(draft 동결). 🔴 **exactly-once**=6a 미러(재시도 시 동일 id set=update→storeInbound update 규칙 거부→배치 전체 거부→재차감 불가). **단일 원자 배치**(청크 절대 금지, codes+lines>450 차단). 프리플라이트(`_adjReadDoc` 재사용, `{OUT}_0001` 서버read=착지=성공처리) + `_saleCommitWithTimeout` 15s + denied 착지 재확인 + `_outInFlight` 가드(finally release). **방향별 draft 분리**(`_inbDraftKey` 방향인지: 입고=`lemango_inbound_draft_{store}` 기존 키 불변 / 반출=`lemango_outbound_draft_{store}`; 반출 draft 는 pendingOutNo 포함) — 전환 시 현재 리스트 자기 draft 저장 후 새 방향 로드(swap) → +qty 라인이 -qty 로 재해석 구조적 불가(설계 §3.1/§7-C15). **로케이션=참고용**(반출 확정 시 `sizeLocations` 안 씀; commit/인라인편집 빈값 허용 — 입고만 Rule3 필수). **Q3 재고부족=경고+진행**(staging 라인 현재재고 표시 + 부족 빨강 `inb-list-neg-row` + 확정 korConfirm 이 음수 라인 열거). 반출 모드 시각 구분(헤더 `#7a1f1f` 틱·반출 배지·확정버튼 danger·타이틀 `📤 반출 스캔`). **입출고 내역**=반출 유형 필터(`__outbound__`)·음수 증감(6a `_mvSignedQty`)·`_mvTypeLabel`='반출'·OUT- 번호·엑셀 부호합; **취소**=6a 제네릭 `confirmInbCancel`(stockDelta 부호반전 `increment(-stockDelta)`=`+qty` 복원, 재고 되돌림). logActivity('outbound','반출'). **정상입고 흐름 byte-unchanged**(공유함수 inbound 분기 동작 동일, 입고 write 경로 무변). 규칙/인덱스 변경 없음(이동 doc=storeInbound create 규칙, 6a 인덱스 재사용) → `--only hosting`. 다음=6c 불량재고.
- 07-06: **POS Phase 6c 불량재고(Defect · scope C=전환+표시+불량반출)** — `storeStock` 에 **`defectSizes` 병렬 버킷**(판매불가) 추가. 🔴 **판매 격리(구조적)**: reader 감사 결과 `loadStoreStock`(core.js) 만 raw doc 읽고 **필드 화이트리스트** → defectSizes 는 여기+`buildStoreStockIndex` 에 명시 추가한 곳만 노출. `getStoreStock`=sizes only(불변) · 신규 `getStoreDefect`(`_storeDefectIndex` 별개 인덱스). 판매확정/void/1f/입고/반출/재고수정 sellable math 는 전부 `sizes` 만 → 불량이 판매로 새지 않음(코드 변경 0). **정상↔불량 전환**=재고수정(6a) 창 per-line **op 선택**(재고조정±/정상→불량/불량→정상, `_adjLineDeltas`): 전환 라인=`sizes∓N`+`defectSizes±N` **한 storeStock merge-set 동시 increment**(원자) + 이동 doc `moveType:'defect-in'|'defect-out'` **듀얼 델타** · 사유 필수 · ADJ 결정적 id · 단일 배치(청크 금지) · exactly-once. 순수 재고조정 세션=**6a byte-unchanged**(defectSizes 키 안 씀, moveType 'adjust', defectDelta 0). **불량반출**=반출 스캔(6b)에 **3번째 방향** `defect-outbound`(`_inbIsOut()` 로 반출 계열 통합): `defectSizes:increment(-qty)`(정상 sizes 무관)·moveType `defect-outbound`·stockDelta 0/defectDelta -qty·OUT-·exactly-once·방향별 3-draft 분리(`lemango_defect_outbound_draft_`). 정상반출은 불변(sizes -qty). **표시(Q7)**: 1f 불량 배지(defect>0, `getStoreDefect`) + 상세 모달 사이즈별 정상/불량/로케이션 3컬럼. **입출고 내역**: 불량증감 컬럼(`_mvDefectDelta`, COLS 10→11) + 유형 필터(정상→불량/불량→정상/불량반출, `MV_FILTERS`) + 요약 불량 순변동 + 엑셀 불량증감 컬럼. `_mvTypeLabel` 3라벨 추가. **취소(Q8)**: `confirmInbCancel` 트랜잭션이 **stockDelta·defectDelta 둘 다** 부호반전(`increment(-stockDelta)`+`increment(-defectDelta)`; defect-outbound=stockDelta 0→sizes increment(0) 무해+defect 복원) — 6a 부터 전방호환이라 트랜잭션 **무변경**, 미리보기만 불량 추가. **규칙/인덱스 변경 없음**(storeStock write 규칙 field-agnostic → defectSizes 무규칙 · 이동 doc=storeInbound create 규칙) → `--only hosting`. 다음=6d 통합뷰+baseline.
- 07-06: **POS Phase 6d 품목 이동 원장 + 기준재고 + 대조 (Phase 6 캡스톤 · ✅ Phase 6 완료)** — 품번+기간별 **통합 타임라인**(입고/판매/판매취소/반출/재고수정/정상↔불량/불량반출/기준) 단일 뷰. **쿼리 전략**: 이동=`storeInbound(storeId,productCode,dateKey)`[6a 인덱스] 품번당 저렴 · 판매/취소=`storeSales(storeId,dateKey)`[3a 인덱스] **기간 스캔 + 클라 `lines[]` 필터**(embedded lines→품번 쿼리 불가, 설계 Q5 MVP; 비용 ∝ 매장 기간 판매량, >120일 경고). 판매→−qty·취소→+qty 행. 병합·`confirmedAt/soldAt/voidedAt` ASC 정렬. **누계(잔여)**: baseline 행=리셋(절대 origin), 취소 이동=제외(취소 트랜잭션이 이미 역반영), 이후 ±합산 → 기준 이후 절대 재고. **🔴 대조(정직)**: 이론치=최신 baseline(명시 or **암묵 0**) + 그 시점(baselineTs, 한 run=confirmedAt 공유) 이후 ΣstockDelta vs 실재고 `getStoreStock`; **기준 있고 기간이 오늘까지 커버할 때만** 판정(일치=초록/불일치=장부X·실Y 빨강 양수표기) · 기준 없으면 "기준 미설정—대조 불가"(**가짜 일치 절대 없음**) · 오늘 미포함=range-incomplete 안내+[기준일~오늘] 버튼 · 불량도 defectDelta 병행 대조. 🔴 **암묵 0 대조**(리뷰 지적 수정): 기준 시점 0이라 baseline doc 없던 사이즈도 전 SIZES 대조에 포함(theo 0) → 원장 우회 변경이 녹색 헤더 아래 숨는 사각지대 제거. 재기준 시 최신 run 의 baseline 만 앵커(`b.ts>=baselineTs`). **baseline(관리자 [🧭 기준 재고 설정])**: 비어있지 않은 (품번,사이즈)마다 `moveType:'baseline'` SET origin(stockDelta=현재 정상, defectDelta=현재 불량) doc **결정적 id `{BASE-…}_{code}_{size}`**(baseNo=`BASE-`+kstStamp+접미사, 재실행마다 새 번호), **청크 450**(SET origin이라 부분완료 무해·재실행=대체; 상쇄 세션 아니라 청크 안전 — 재고수정과의 의도적 차이). **재실행 supersede**: 대조/누계가 사이즈별 **최신** baseline(리셋)만 사용 → 이중계상 없음. grade≥3·per-store·KST·가드 `_ldgBaselineInFlight`. **진입**: 재고현황 허브 [📊 품목 원장](전 직원) · 상세 모달 [📊 이동 원장] · [🧭 기준 재고 설정](관리자). baseline 이동은 **2d 입출고 내역에서 제외**(`_inbHistApplyFilters` 초입, 순변동 왜곡·오취소 방지). 엑셀(타임라인+대조요약). **판매 write 경로 완전 무변경**(스캔 읽기전용, buildSaleDoc/확정/void 무손). **규칙/인덱스 변경 없음**(6a·3a 인덱스 재사용, storeInbound create 규칙 field-agnostic) → `--only hosting`. **✅ Phase 6(6a 재고수정→6b 반출→6c 불량→6d 통합뷰+baseline) 완료.** 남은 6e(판매 미러=전기간 정확 대조)는 옵션.
- 07-06: **POS Phase 5-4 콤보/번들 (조합 할인 · ✅ Phase 5 매장할인 완료)**(🟢) — 마지막 할인 조각. 5-3 split-line+lineId 인프라 재사용. ① **조건 `combo{items[{scope,ref}],eachQty}`·`bundle{...}`**(멤버 전부 present)·**혜택 콤보=`percent`/`amount` · 번들=`bundlePrice{price}`**(설계는 codes[] 상품만 → scope 멤버로 확장). ② **파이프라인 line→quantity→combo/bundle→cart**: pass1.7 `_saleApplyComboBundle` — 미claimed 유닛(manual/수량promo 제외)으로 인스턴스 반복 형성(멤버별 eachQty **최저가 예약**, temp avail 로 멤버 중복 방지, instances=min floor(avail/eachQty)) → **combo**: %/정액 disc · **bundle**: S(예약 유닛 판매가 합)−price → per-unit 분배(비례 floor+**잔액 최고가**, share≤price 클램프) → 부모 `_bundleGroups`(instanceId+share 그룹). ③ 🔴 **effective 라인**: 콤보/번들 서브라인 추가(각 균일 unitDiscount), bundle=`bundleInstanceId` 태그. cb 유닛=카트 제외(`_cbKind`, 결합 promo 종결·번들 고정가 보존). ④ 🔴 **번들 whole-instance-void-only**(오너 확정): 취소모달이 번들 멤버 **부분취소 차단**(qty 입력 대신 "번들 전체" 체크박스, `onSvBundleToggle` 같은 instanceId 동기) → `_svCollectSelections` 가 체크된 번들=남은 전량 selection → **기존 원자 트랜잭션 그대로**(머니/트랜잭션 byte-unchanged, UI 제약만). **콤보(%)=부분취소 per-unit 스냅샷 허용**. ⑤ 🔴 **머니/재고 byte-unchanged**(검증: 번들 A+B+C+D 고정 50000→정확 분배 합 50000·2인스턴스=100000·재고 4멤버 전체차감·전체취소 환불 50000+재고 복원; 콤보 A+B+C 15%=9750). ⑥ **표시**: 라인 🎁 콤보/번들 칩 + 적용행사 요약(effective appliedRuleId 집계) + 번들 취소 힌트. ⑦ **Settings**: 조건 콤보/번들 + 멤버 `scope:ref` 콤마입력(≥2)·eachQty + 혜택(콤보 %/정액·번들 고정가) + 검증(멤버 존재·번들가>0·정상가합 이상 경고). `getActiveDiscounts` 무변경. 매출공식·6a~6d·1f·3c/3e 머니 무손. code-reviewer 🟢. **규칙/인덱스 변경 없음 → `--only hosting`. ✅ Phase 5(5-1~5-4) 완료.**
- 07-06: **POS Phase 5-3 N+1 · 두번째반값(수량 promo, split-line + lineId)**(🟢) — 5-2 엔진에 **수량조건**과 **split-line 아키텍처** 추가. ① **조건 `qty{scope,ref}`**(scope=product/brand/category)·**혜택 `nplus{buy,free}`(N+M 증정)·`secondHalf{nth,value}`(n번째 %)**. ② **파이프라인 line→quantity→cart**: pass1.5 `_saleApplyQtyDiscounts` — 자격 유닛(manual-override 제외, 라인할인 후 단가) 확장→🔴 **최저가 유닛부터** 무료(nplus: floor(units/(buy+free))×free)/할인(secondHalf: floor(units/nth)) 선택(오너 확정), **v1 최대절감 1개 no-stack**(카트레벨 미러), 부모 라인에 `_qtyFree/_qtyHalf/_qtyHalfVal/_qtyRule*` breakdown. ③ 🔴 **split-line**(`_buildEffectiveLines`→`_saleEffLines`): 부모 → 정상(paid)/반값(half)/무료(free) 서브라인(각 균일 unitDiscount) — 총계/표시/카트/확정 소스. **같은 (품번,사이즈) split 충돌 해결**: split 시 `lineId`(`{code}#{size}#{n}`) 부여 → 🔴 **부분취소/집계/취소UI/누적상한 트랜잭션을 `_lineIdOf(l)=lineId||(code,size)` 로 전환**(오너 승인 Option A). **레거시/비-split 판매=lineId 없음→(code,size) 폴백=byte-identical**(회귀 0). 무 promo=부모 1:1→5-1/5-2 무손. ④ 🔴 **머니 byte-unchanged**: 모든 할인(라인+수량+카트)=effective 라인 unitDiscount 폴딩 → buildSaleDoc totals·buildPartialVoidDoc·3c 확정 무변경(검증: split A/M×3 2+1→A#M#0 유료×2 + A#M#1 무료×1, 판매 20000, 무료 라인 취소=0원·유료=10000원, 누적상한 라인 독립). ⑤ 🔴 **무료 유닛도 재고 차감**(확정=effective 라인 code/size별 qty 합산=전체 qty, 무료 포함 — 물리 반출). buildSaleDoc/재고 increment 무변경. ⑥ **표시**: 부모 라인 1행(편집) + 🎁 수량 promo 배지(N개 무료/반값) + effective 반영 금액; 판매 doc/영수증/취소모달=split 라인(lineNote 무료/반값). split 라인은 엔진 파생(qty 편집=부모 재파생, 재평가 트리거는 5-2 핸들러 재사용). ⑦ **Settings**: 조건 '수량(N+1·반값)' + scope/ref + 혜택 nplus/secondHalf(buy/free·nth/value) + 검증(`.disc-ct-qty`/`.disc-bval2` CSS 토글). `getActiveDiscounts` condition-agnostic 무변경. 매출공식·6a~6d·1f 무손. code-reviewer 🟢. **규칙/인덱스 변경 없음 → `--only hosting`.** 다음=5-4.
- 07-06: **POS Phase 5-2 카테고리/브랜드 + 총액(카트레벨 분배)**(🟢) — 5-1 엔진을 2가지로 확장. ① **라인레벨 조건 추가**(pass1): `brand`(상품 brand 필드)·`category`(상품 **type** 필드 — 검증: brand 전 상품·type=onepiece/bikini/goggles/wetsuit/rashguard/monokini/kids/accessories) — `_ruleMatchesLine` 확장, product 와 동일하게 per-line 매칭→최대절감 1개→`unitDiscount`(percent/fixed). ② 🔴 **첫 카트레벨**(pass2 `_saleApplyCartDiscounts`): `cartTotal{minTotal}` 조건, 라인할인 후 **소계≥minTotal** 시 최대절감 1개(benefit `amount{minus}`정액 / `percent`), `_distributeCartDiscount` 로 자격 라인들 `unitDiscount` 에 **per-unit 폴딩 분배**(비례 floor + 잔액 최고가 라인, Q8). 🔴 **분배=unitDiscount 폴딩(별도 머니필드 없음)** → `buildSaleDoc` totals·`buildPartialVoidDoc`·3c/3e/부분취소 **byte-unchanged**(검증: 부분취소=voidQty×(unitPrice−unitDiscount) per-unit, 카트 **재계산 없음**=판매시 스냅샷). `cartDiscount`(per-unit 카트몫)·`cartRuleId/Name` = **표시·귀속 전용 additive**(_buildSaleLine 조건부 부착). `buildSaleDoc.appliedDiscounts` = 라인/카트 **분리 귀속**(level:'line'|'cart', unitDiscount−cartDiscount=라인몫). ③ **라인+카트 병존**(across-level): 브랜드15% 라인도 소계에 포함되고 카트 분배 몫도 받음(작업지시 확정) — 각 레벨 내 no-stack, **manual-override=라인+카트 모두 제외**(Q3 확장). ④ **분배 정확성**: qty=1 자격라인 있으면 Σ=S 정확(테스트 3라인/10000→3334/3333/3333); 전 라인 qty≥2 시에만 ≤(min qty−1)원 매장유리 잔여(요약은 실제 분배액 표시=정직). ⑤ **표시**: 스캔 카드=brand/category 적용가 · 라인 배지 🏷라인+🛒카트 칩 · "적용 행사" 요약 라인/카트 분리(🛒). ⑥ **Settings**(매장 할인 상품 관리 서브탭): 조건종류 select(상품/브랜드/카테고리/총액) → 동적 필드(`_discCondToggle`, `.disc-ct-*` CSS 토글=인라인 display 미사용) + 혜택 select(총액=%/정액·그 외=%/특정가) + 검증. `getActiveDiscounts`=condition-agnostic 무변경. 매출공식(Cafe24/사방넷)·재고차감·6a~6d·1f 무손. code-reviewer 🟢. **규칙/인덱스 변경 없음 → `--only hosting`.** 다음=5-3.
- 07-06: **POS Phase 5 매장 할인 심층 설계 문서**(`docs/pos-phase5-discount-design.md`, 코드 없음) — **condition×benefit 프레임워크**(조건: 상품/카테고리/브랜드/총액/수량/조합 · 혜택: %/특정가/정액/N+1/두번째반값/번들고정가 · period·priority·scope). 핵심 결정: ① **규칙 저장=`sharedData/storeDiscounts` 설정 배열**(inboundTypes 패턴 재사용, 규칙 1블록 admin-write 추가 — 5-1 유일 규칙변경; 컬렉션 아님). ② 🔴 **앵커=전부 per-line `unitDiscount` 로 해소**(카트레벨·번들도 라인 분배, 정수 KRW 잔액=최고가 라인) → `buildSaleDoc` totals·`buildPartialVoidDoc`·3c/3e 머니 math **byte-unchanged**(`discountSource:'store-discount'` + `discountRuleId/Name` 라인 기록 + doc-level `appliedDiscounts` 요약, 전부 additive). ③ **충돌=가장 유리한 1개(max 절감), no-stack v1**, 라인 vs 카트 배타(consumed). ④ 🔴 **번들 부분취소=전체번들취소만 v1**(bundleId 태그, 5-4 에서만; 대안 정가환원/분배유지 제시). ⑤ 엔진=매 스캔/카트변경/draft로드 재실행(stale 힌트, 기존 sale-line-stale 재사용), 수동 편집=`manual-override` 고정. 서브분할 **5-1(상품별 %/특정가+기간자동, 3c/3e 무손)→5-2(카테고리/브랜드+총액 정액, 라인분배)→5-3(N+1/두번째반값, split-line)→5-4(콤보/번들+환불정책)**. 11개 크리티컬 분석(카트레벨 라이브표시·충돌 결정성·% 반올림·번들환불·규칙변경 vs draft·KST 기간경계·성능·레거시 무태그). 오픈질문 8개(추천 포함). 배포: 5-1=`--only firestore:rules,hosting`(규칙 1블록), 이후 대부분 `hosting`. 쿠폰/결제수단할인=프레임워크가 표현 가능(조건 타입 추가만)하나 범위 밖.
- 07-06: **POS Phase 5-1 상품별 할인(%/특정가 + 기간 자동)** — `_storeDiscounts` 설정(core.js, inboundTypes 패턴: localStorage `lemango_store_discounts_v1` + `_fsSync('storeDiscounts')` + FORCE payload + `_fsReloadSharedSettings` + `_onSharedDataChanged` switch). **규칙 저장=`sharedData/storeDiscounts`**(admin-write OR-union: 일반 규칙에서 docId 제외 + 전용 블록 — 🔴 **유일한 규칙 변경**). 설정 [🏷 매장 할인 관리] 카드(grade≥4): 규칙명·품번(datalist)·혜택(％/특정가)·값·기간·매장 scope · CRUD(add/edit/toggle/delete) · 검증(%1~100·특정가>정상가 경고·start≤end·품번 존재). 🔴 **자동 적용 엔진**(`_saleApplyDiscounts`): 스캔/카트변경/draft로드마다 활성+기간(KST)+매장 통과 규칙 중 **라인별 최대 절감 1개**(Q5 no-stack, tiebreak priority) → `unitDiscount` 채움 + `discountSource:'store-discount'` + `appliedRuleId/Name` 스냅샷. percent=`floor(단가×%)`·fixed=`max(0,단가−특정가)`(Q8 per-unit floor 정수 KRW). 매칭 없으면 store-discount 해제. **Q3 수동 잠금**: `onSaleLineDiscount` 편집 → `discountSource:'manual-override'` 잠금(엔진 스킵, 재스캔 무복원, draft 보존). **표시(Q4)**: 라인 행사명/수동 배지 · 스캔 카드 행사 적용가(취소선) · 하단 "적용 행사" 요약(`saleAppliedPromos`, 5-2 카트레벨 재사용). 🔴 **앵커=3c/3e/부분취소 머니 math byte-unchanged**: 엔진은 `unitDiscount` 만 채움 → `buildSaleDoc` totals(판매가=정상가−할인가)·`buildPartialVoidDoc`(원단가 per-unit 재계산) 무손. `_buildSaleLine`=appliedRuleId 조건부 부착(Firestore undefined 거부 회피) + discountSource 화이트리스트 3값. buildSaleDoc=`appliedDiscounts` 요약(additive). 매출공식(Cafe24/사방넷 revenueLog)=무관. logActivity('setting','매장할인'). **배포=`--only firestore:rules,hosting`.** 다음=5-2.
- 07-06: **POS 5-1 할인 관리 UI 이전 (설정 카드 → 🏬 매장 '매장 할인 상품 관리' 서브탭)**(🟢) — **순수 relocation, 신규 할인 기능 없음**. 이전 이유: 할인은 매장별(storeScope) 가능 → 매장 컨텍스트(`resolveActiveStore`)와 함께 두는 것이 자연스러움(매출 조회 3e 승격과 동일 논리). ① **settings.js 제거**: `discountSection`(HTML 빌더 60여줄)·`${discountSection}` 참조·CRUD 6함수(`_discReadFields`/add/edit/save/toggle/remove)·window 노출 전부 삭제 → 주석만 잔존(orphan 0, grep clean). ② **store.js 이전**: `renderStoreDiscountPanel()`(온디맨드 본문 렌더, `#storeDiscountBody` 없으면 no-op) + CRUD 6함수(renderSettings→`renderStoreDiscountPanel` 재렌더로 교체) + `_storeSubPanelHtml` discount 브랜치(1c "준비중" placeholder 대체) + `renderStoreTab`/`switchStoreTab` dispatch. ③ **권한**: 서브탭 조회=전 직원(패널 진입 가능) · **관리(추가/수정/삭제/토글)=grade≥4**(`_discCanManage`, 설정 카드 isTopAdmin 게이트와 동치) — 비관리자는 actions/editRow/addRow 미출력 + "조회만 가능" 안내, 각 mutation 함수도 grade 가드(방어). ④ **매장 컨텍스트 연결**: 신규 규칙 add-row `storeScope` 기본값=`_discDefaultScope()`(resolveActiveStore 활성매장이면 그 매장, 아니면 'all') — 모델(all|storeId)은 5-1 그대로, 기본값만 합리화. ⑤ 🔴 **NO 인라인 display:none**: 편집행 표시/숨김을 인라인 `style="display:none"`→CSS 클래스 토글(`.disc-item.disc-editing`)로 전환. 특이도 `.disc-item .disc-editrow`(0,2,0)>`.set-item-editrow`(0,1,0) → 소스순서 무관 기본 숨김, `.disc-item.disc-editing .disc-editrow`(0,3,0) 편집 시 표시. ⑥ **실시간 동기화**: core.js storeDiscounts 훅이 `renderStoreDiscountPanel()` 추가 호출(패널 없으면 no-op). 🔴 **엔진/판매경로/규칙 byte-unchanged**: `_saleApplyDiscounts`·`buildSaleDoc`·`buildVoidDoc`·부분취소·`_buildSaleLine`·core.js 데이터층(`getActiveDiscounts`/`saveStoreDiscounts`/`generateDiscountId`)·Firestore 규칙 전부 무손(diff 확인). `_discReadFields` 검증(%1~100·특정가 clamp·start≤end) 원본 identical. code-reviewer 🟢. **규칙/인덱스 변경 없음 → `--only hosting`.**
- 07-03: **POS Phase 6 심층 설계 문서**(`docs/pos-phase6-design.md`, 코드 없음) — 재고수정(실사)/반출/불량재고/입출고현황(통합원장) 4기능을 1시스템으로 설계. 핵심 결정: ① **record 아키텍처 = `storeInbound` 를 per-line 이동 원장으로 확장**(moveType + signed stockDelta/defectDelta; 규칙 무변경 — create/cancel 규칙에 필드 화이트리스트 없음; 신규 복합인덱스 `(storeId,productCode,dateKey)` 1개만). 대안 새 `storeMoves` 컬렉션은 Q1. ② **baseline 정합성(최난제)**: 1e SET 업로드가 movement 기록 없이 `storeStock.sizes` 직접 덮어씀(`store.js:496-504`) → 이동만으로 절대재고 복원 불가. 관리자 1회 **기준 스냅샷 movement**(moveType:'baseline') 후 `storeStock == baseline + ΣstockDelta` 정확 정합; 그 전엔 "기준 미설정(비교 불가)" 정직 표시 + 향후 업로드도 movement 발행. ③ **판매 per-product 쿼리 불가**(판매 라인 embedded `store.js:1838`) → 통합뷰는 기간 스캔 MVP(판매 코드 무변경) + 선택적 6e 미러(3c/3e 원자배치에 결정적-id movement 추가, exactly-once 유지). 불량=Option A(`defectSizes` 맵, 규칙 무변경, 판매는 sizes만 건드려 격리 확인). 서브분할 6a(재고수정=앵커, 스키마 확정)→6b(반출=스캔 방향토글)→6c(불량)→6d(통합뷰+baseline)→6e(옵션 판매미러). 15개 크리티컬 분석(offset 세션 단일배치 필수, 레거시 backfill, 반출 음수, 사유 규칙 강제 불가=클라 신뢰모델). 오픈질문 8개(추천 포함). 배포계획: 6a=`indexes,hosting`, 나머지 대부분 `hosting`.
- 07-03: **재고관리 다운로드 WIDE→LONG(바코드 관리용)**(🟢) — 재고 관리 탭 [⬇ 다운로드](`downloadExcel('stock')`, excel.js stock 분기)를 WIDE(`품번|상품명|브랜드|판매가|XS…F|합계`, 시트 재고조회, 바코드 표시 불가)에서 **LONG**(`품번|사이즈|바코드|상품명|브랜드|판매가|수량`, 상품×사이즈 1행, 시트/파일 바코드관리)로 교체. 바코드 미등록 사이즈=공란 → 등록현황 가시화(핵심). 앞 3열이 바코드 일괄 업로드 파서(index r[0]/r[1]/r[2])와 호환 → 다운로드→채움→재업로드 라운드트립(mockup 순서 `품번|상품명|…|사이즈|바코드`는 업로더 index 불일치라 라운드트립 위해 재정렬). data=`State.stock.filtered`(정렬/필터 보존). 데이터모델/업로더/역인덱스/타 export 무변. `--only hosting`.
- 07-03: **generateSaleNo 접미사 강화**(🟢) — 판매번호=판매 doc id(3c) 인데 구 접미사가 2자(uid 파생 1자 결정적 + 랜덤 1자 → **같은 계정 36조합**)라, 한 계정으로 2대 이상 단말 운영 시 **동일 초 확정 1/36 충돌 → 2번째 판매 doc id 중복 → batch 거부 → 3c 판별이 "이미 착지"로 오판 → 판매 조용히 유실** 경로(리뷰어 지적, 3c 이래 잠복). 접미사를 8자 `<ms3><세션카운터2 base36><랜덤3 base36>` 로 강화(`_kstParts().ms` 추가, `generateSaleNo` 가 now 1회에서 초·ms 동시 산출 — KST 산술 유틸만, Intl/로컬 비의존). 충돌확률: 동일-초 교차단말 ≈ 1/(1296×46,656)≈1.65e-8/쌍. 실측 200k 동일단말 tight-loop 0중복 + 1M 동일-ms 2단말쌍 0충돌. saleNo=`SL-\d{8}-\d{6}-[0-9A-Z]{8}` 유효 Firestore id. **exactly-once/batch/규칙/void 스킴 불변 — 문자열 조성만 변경.** 표시는 soldAt 기반이라 무영향. 규칙/인덱스 변경 없음 → `--only hosting`.
- 07-03: **부분 판매취소(라인/수량 선택)**(🟢) — 전량취소만 가능하던 3e 를 부분취소로 확장. 회차별 doc `void_{saleId}_{NNNN}`(seq 연속), 원본 판매 불변(append-only). 🔴 **누적 상한 트랜잭션**: 웹 SDK 는 트랜잭션 내 쿼리 불가 → 판매 doc + 취소 doc들을 **결정적 seq id 순회 read**(seq≤판매수량+2) → 라인별 누적 취소수량 계산 → (누적+요청)≤판매 검증 → 초과 abort. 동시 취소 = 같은 seq write→트랜잭션 read-conflict 재실행→재검증 → **over-void 불가**. **멱등 토큰**(clientToken, 회차 단위): 모호 타임아웃 재시도 시 같은 토큰 취소 존재하면 no-op → within-cap 중복복원 방지(3c 전량 결정적 id 미러의 부분판 대응). 재고 복원=`increment(+voidQty)` 트랜잭션 원자, `sizeLocations` 불변. 취소 다이얼로그=라인별 판매/이미취소/남은 + 취소수량 입력 + [전체취소] + 라이브 합계 + 사유 필수(클라 trim + 서버 규칙). 권한=`_svCanVoid`(관리자/본인매장), 남은>0 일 때만 버튼. 검출=`where('originalSaleId','in',30청크)`(단일필드 auto 인덱스) → 판매별 취소 집계(`_shAgg`: 상태 정상/일부취소/전체취소, 남은 금액/수량). 리스트=정상/일부취소(남은 ₩X)/취소됨. 영수증=라인별 판매/취소/남은 + 취소 이력(회차별). 엑셀=판매(+) & 각 회차 취소(−) 동일 판매번호 → 판매가 합계=남은 실현매출. **요약 순액 변경**: Σ(판매−취소)=남은(전량제외 아님). 규칙/인덱스 변경 없음(void create 규칙 id-agnostic, 3a 선배포) → `--only hosting`. `_saleReadSaleDoc`(3c) 프리플라이트 재사용, 20s 트랜잭션 타임아웃→안전 재시도. **다음=강제 재고 차감/Phase 4 환불**.
- 07-03: **3e 후속 버그픽스 3종**(🟢, 소유주 라이브 엑셀 테스트) — ① 🔴 **KST/UTC 시각 버그**: 전 시각/dateKey/번호 생성이 `Intl('Asia/Seoul')` + **로컬시간 catch 폴백**에 의존 → ICU 없는 런타임/UTC 기기에서 UTC/로컬로 새어 saleNo·일시 ~9h 밀림, **dateKey 매출 귀속일 오류 위험**(00:00~09:00 KST 판매가 전일로 귀속). → **산술 KST 단일 유틸**(`kstDateKey`/`kstStamp`/`kstFormat`, UTC+9 `getUTC*`, Intl 비의존)로 `generateSaleNo`/`_inbDateKeyKST`/`_inbInboundNo`/`_inbHistDateTime` 전면 교체. soldAt/voidedAt=UTC instant 유지(표시만 KST). ② **취소 행 음수화(엑셀)**: 취소 행 수량/정상가/할인가/판매가 음수(단가/할인단가는 per-unit 유지) + 취소된 판매의 짝 취소행도 방출 → **열 합계=실현매출**(판매+ 취소− 순액 0). 리스트 반품행은 이미 음수. 요약(취소 제외)은 부호 변경과 독립(무영향). ③ **취소↔원판매 연결**: 취소 행/엑셀 판매번호 = **원 판매번호**(`_shOrigSaleNo`, originalSaleNo→없으면 `void_` 제거 폴백) → 판매+취소 쌍 동일 번호로 그룹핑, 영수증에 "원 판매번호 …" 명시. 페어링 이상(#4)은 서로 다른 판매(존재조회 id 기반 정합, KST 버그 무관)로 진단 — 코드 버그 아님, 표시/시각 개선으로 해소. **테스트 레코드 dirty**(소유주 go-live 전 리셋 예정, 미마이그레이션).
- 07-03: **Phase 3e 판매취소 라이브 + 매출 조회 서브탭 승격**(🟢) — **Phase 3 완료(finale)**. ① 매출 조회를 3d 모달에서 `🏬 매장 → '매출 조회'` **전용 서브탭**(STORE_SUBS 판매↔재고현황 사이)으로 승격: UI를 패널로 직접 렌더(renderSalesHistoryPanel, 진입 시 오늘~오늘 자동 조회), salesHistoryModal 다이얼로그 제거(영수증 상세 saleReceiptModal만 모달 유지), 재고현황 허브의 [📊 매출 조회] 버튼 제거, 판매 화면 헤더 링크는 `switchStoreTab('sales')`로 전환. 조회 전 직원 개방(작업 게이트 없음). ② **판매취소(void)**: 영수증 [판매 취소] → saleVoidModal(요약 + 재고복원 미리보기 `+qty` + **사유 필수** + 파괴버튼 비-기본포커스). 단일 `db.batch()`: `storeStock` `increment(+qty)` 코드별 그룹 merge-set(복원, `sizeLocations` 불변) + `storeSales` **void doc(id=`void_{saleId}` 결정적, buildVoidDoc)** → 재시도 시 기존 void doc=`update` 평가 → 규칙 `update:false` → 배치 전체 거부 → **double-void 구조적 불가**(재복원 없음). 원본 판매 doc **불변**(append-only). 사유 필수(클라 trim + 서버 규칙 backstop), 권한 `_svCanVoid`=관리자 OR 본인 매장(office/타매장=버튼 숨김+실행 차단, 서버 최종 방어). 프리플라이트(onLine + void doc 서버read 5s, 존재 시 이미취소 성공처리) + 15s 커밋 타임아웃(모호→안전 재시도 안내) + permission-denied 착지 재확인. 성공 시 `logActivity('sale-void',…)` + `_shVoidByOrig` 로컬 갱신 → `_shApplyFilters()`(취소선/요약 재계산, 재조회 없이) + 영수증 재오픈(취소 패널) + 1f 재고 갱신. 가드 `_svInFlight`(finally release). `_saleReadSaleDoc`/`_saleCommitWithTimeout`(3c) 재사용. 규칙/인덱스 변경 없음(void create 규칙 3a 선배포). **다음=강제 재고 차감/Phase 4**.
- 07-03: **Phase 3d 매출 조회**(🟢) — 읽기 전용(쓰기 0건), 조회 전 직원 개방. 재고현황 허브 툴바 [📊 매출 조회] + 판매 화면 헤더 링크(salesHistoryModal, saleReceiptModal, main.js draggable 등록). 기간 조회: `storeSales.where(storeId==).where(dateKey>=).where(dateKey<=)` → 3a 복합인덱스(storeId,dateKey) EXACT, orderBy 없음·클라 soldAt DESC 정렬. 결제수단/상태 필터 = 메모리(인덱스 불변). 🔴 **취소상태 정합성(money-critical)**: 판매 doc 의 취소 여부를 취소 doc(id=`void_{saleId}`) **직접 존재조회**(`where(FieldPath.documentId(),'in',[…])` 30개 청크; 범위 내 취소 먼저 매핑 후 미커버 판매만 조회 → 읽기 최소화)로 판별 → 판매와 **다른 날** 취소(dateKey 범위 밖)도 정확히 '취소됨' 마킹. 요약("기간 판매 N건·매출·할인·수량, 취소 제외")은 **비취소 판매만** 집계(결제수단 필터 반영, 상태 필터는 표시 렌즈). orphan void(원본 판매 범위 밖)=반전(반품) 행으로 표시. 번호 검색: `customerPhone==digits`(전 매장·전 기간, 단일필드 auto 인덱스, **전체번호 표시**=마스킹 유일 예외; 그 외 전부 maskPhone). 영수증 상세 모달(saleNo/일시/매장/작업자/결제/휴대폰 + 라인표 품번·상품명·사이즈·수량·단가·할인단가·판매가 + 합계 + 취소상태 + [판매 취소] **3e 스텁**(비취소 판매만 표시, 클릭=토스트, 쓰기 없음); 품번 click→openStoreProductDetail 공용). 엑셀=라인당 1행 16컬럼(현재 뷰, 휴대폰 마스킹), 파일명 `매출내역_{매장}_{시작}~{끝}.xlsx`/`매출내역_번호검색_{번호}.xlsx`. 규칙/인덱스 변경 없음. **다음=Phase 3e 판매취소**.
- 07-03: **Phase 3c 판매 확정 라이브**(🟢) — 단일 `db.batch()`: `storeStock` `increment(-qty)` 코드별 그룹 merge-set + `storeSales` sale doc(**doc id = 판매번호, 결정적**). **exactly-once**: 재시도 시 동일 판매번호 → 기존 doc = `update` 평가 → 규칙 `update:false` → 배치 전체 원자 거부 → 재차감 구조적 불가. `_salePendingSaleNo`(draft 영속, 첫 확정 1회 생성·성공 시에만 소거) → 크래시/리로드 후 재시도 멱등. 사전 확인 다이얼로그(saleConfirmModal: 합계/할인/수량/결제/휴대폰 마스킹 + 음수재고 경고, [확정] 기본 포커스·Enter). 연결 프리플라이트(`navigator.onLine`==false 차단 + 서버소스 read 5s 타임아웃 차단, 착지 시 성공 처리). 15s 커밋 타임아웃→모호(pendingSaleNo 보존, 안전 재시도 안내). permission-denied→서버 재read 판별(exists=이미 반영/not=권한오류). 실패=전체 보존, 성공=해당 매장 list/draft/pendingSaleNo만 소거. 가드 `_saleInFlight`(동기 set + finally release). 가격 스냅샷 stale 힌트(draft 복원 시 현재 판매가와 다르면 노란 표시, 자동갱신 안 함). `generateSaleNo` 접미사 `-XX`(uid1자+랜덤1자, 동일-초 2단말 충돌 완화). 규칙/인덱스 변경 없음 → `--only hosting`. **다음=Phase 3d 매출조회**.
